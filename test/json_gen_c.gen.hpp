// Auto-generated C++ wrapper header — DO NOT EDIT.
// Generated by json-gen-c with --cpp-wrapper.
#pragma once

#include <string>
#include <cstring>
#include <stdexcept>
#include <utility>
#include <vector>

#include "json.gen.h"

namespace jgc {

class Triangle {
public:
    Triangle() { Triangle_init(&data_); }
    ~Triangle() { Triangle_clear(&data_); }

    Triangle(Triangle&& other) noexcept {
        std::memcpy(&data_, &other.data_, sizeof(data_));
        std::memset(&other.data_, 0, sizeof(other.data_));
    }
    Triangle& operator=(Triangle&& other) noexcept {
        if (this != &other) {
            Triangle_clear(&data_);
            std::memcpy(&data_, &other.data_, sizeof(data_));
            std::memset(&other.data_, 0, sizeof(other.data_));
        }
        return *this;
    }

    Triangle(const Triangle& other) {
        Triangle_init(&data_);
        sstr_t buf = sstr_new();
        json_marshal_indent_Triangle(const_cast<struct ::Triangle*>(&other.data_), 0, 0, buf);
        json_unmarshal_Triangle(buf, &data_);
        sstr_free(buf);
    }
    Triangle& operator=(const Triangle& other) {
        if (this != &other) {
            Triangle_clear(&data_);
            Triangle_init(&data_);
            sstr_t buf = sstr_new();
            json_marshal_indent_Triangle(const_cast<struct ::Triangle*>(&other.data_), 0, 0, buf);
            json_unmarshal_Triangle(buf, &data_);
            sstr_free(buf);
        }
        return *this;
    }

    // Field accessors
    float base() const { return data_.base; }
    void set_base(float v) { data_.base = v; }
    float height() const { return data_.height; }
    void set_height(float v) { data_.height = v; }
    std::string label() const {
        if (!data_.label) return std::string();
        return std::string(sstr_cstr(data_.label), sstr_length(data_.label));
    }
    void set_label(const std::string& v) {
        if (!data_.label) data_.label = sstr_new();
        sstr_clear(data_.label);
        sstr_append_of(data_.label, v.data(), v.size());
    }
    void set_label(const char* v) {
        if (!data_.label) data_.label = sstr_new();
        sstr_clear(data_.label);
        sstr_append_cstr(data_.label, v);
    }

    /// Serialize to string.
    std::string marshal() const {
        sstr_t buf = sstr_new();
        json_marshal_indent_Triangle(const_cast<struct ::Triangle*>(&data_), 0, 0, buf);
        std::string result(sstr_cstr(buf), sstr_length(buf));
        sstr_free(buf);
        return result;
    }

    /// Deserialize from string. Throws on error.
    static Triangle unmarshal(const std::string& data) {
        Triangle obj;
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_Triangle(in, &obj.data_);
        sstr_free(in);
        if (rc != 0) throw std::runtime_error("unmarshal failed");
        return obj;
    }

    /// Deserialize into this object. Returns true on success.
    bool unmarshal_into(const std::string& data) {
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_Triangle(in, &data_);
        sstr_free(in);
        return rc == 0;
    }

    /// Access the underlying C struct.
    struct ::Triangle& c_struct() { return data_; }
    const struct ::Triangle& c_struct() const { return data_; }

    bool operator==(const Triangle& other) const { return marshal() == other.marshal(); }
    bool operator!=(const Triangle& other) const { return !(*this == other); }

private:
    struct ::Triangle data_;
};

class AliasBasic {
public:
    AliasBasic() { AliasBasic_init(&data_); }
    ~AliasBasic() { AliasBasic_clear(&data_); }

    AliasBasic(AliasBasic&& other) noexcept {
        std::memcpy(&data_, &other.data_, sizeof(data_));
        std::memset(&other.data_, 0, sizeof(other.data_));
    }
    AliasBasic& operator=(AliasBasic&& other) noexcept {
        if (this != &other) {
            AliasBasic_clear(&data_);
            std::memcpy(&data_, &other.data_, sizeof(data_));
            std::memset(&other.data_, 0, sizeof(other.data_));
        }
        return *this;
    }

    AliasBasic(const AliasBasic& other) {
        AliasBasic_init(&data_);
        sstr_t buf = sstr_new();
        json_marshal_indent_AliasBasic(const_cast<struct ::AliasBasic*>(&other.data_), 0, 0, buf);
        json_unmarshal_AliasBasic(buf, &data_);
        sstr_free(buf);
    }
    AliasBasic& operator=(const AliasBasic& other) {
        if (this != &other) {
            AliasBasic_clear(&data_);
            AliasBasic_init(&data_);
            sstr_t buf = sstr_new();
            json_marshal_indent_AliasBasic(const_cast<struct ::AliasBasic*>(&other.data_), 0, 0, buf);
            json_unmarshal_AliasBasic(buf, &data_);
            sstr_free(buf);
        }
        return *this;
    }

    // Field accessors
    std::string username() const {
        if (!data_.username) return std::string();
        return std::string(sstr_cstr(data_.username), sstr_length(data_.username));
    }
    void set_username(const std::string& v) {
        if (!data_.username) data_.username = sstr_new();
        sstr_clear(data_.username);
        sstr_append_of(data_.username, v.data(), v.size());
    }
    void set_username(const char* v) {
        if (!data_.username) data_.username = sstr_new();
        sstr_clear(data_.username);
        sstr_append_cstr(data_.username, v);
    }
    long created() const { return data_.created; }
    void set_created(long v) { data_.created = v; }
    int id() const { return data_.id; }
    void set_id(int v) { data_.id = v; }

    /// Serialize to string.
    std::string marshal() const {
        sstr_t buf = sstr_new();
        json_marshal_indent_AliasBasic(const_cast<struct ::AliasBasic*>(&data_), 0, 0, buf);
        std::string result(sstr_cstr(buf), sstr_length(buf));
        sstr_free(buf);
        return result;
    }

    /// Deserialize from string. Throws on error.
    static AliasBasic unmarshal(const std::string& data) {
        AliasBasic obj;
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_AliasBasic(in, &obj.data_);
        sstr_free(in);
        if (rc != 0) throw std::runtime_error("unmarshal failed");
        return obj;
    }

    /// Deserialize into this object. Returns true on success.
    bool unmarshal_into(const std::string& data) {
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_AliasBasic(in, &data_);
        sstr_free(in);
        return rc == 0;
    }

    /// Access the underlying C struct.
    struct ::AliasBasic& c_struct() { return data_; }
    const struct ::AliasBasic& c_struct() const { return data_; }

    bool operator==(const AliasBasic& other) const { return marshal() == other.marshal(); }
    bool operator!=(const AliasBasic& other) const { return !(*this == other); }

private:
    struct ::AliasBasic data_;
};

class PreciseInts {
public:
    PreciseInts() { PreciseInts_init(&data_); }
    ~PreciseInts() { PreciseInts_clear(&data_); }

    PreciseInts(PreciseInts&& other) noexcept {
        std::memcpy(&data_, &other.data_, sizeof(data_));
        std::memset(&other.data_, 0, sizeof(other.data_));
    }
    PreciseInts& operator=(PreciseInts&& other) noexcept {
        if (this != &other) {
            PreciseInts_clear(&data_);
            std::memcpy(&data_, &other.data_, sizeof(data_));
            std::memset(&other.data_, 0, sizeof(other.data_));
        }
        return *this;
    }

    PreciseInts(const PreciseInts& other) {
        PreciseInts_init(&data_);
        sstr_t buf = sstr_new();
        json_marshal_indent_PreciseInts(const_cast<struct ::PreciseInts*>(&other.data_), 0, 0, buf);
        json_unmarshal_PreciseInts(buf, &data_);
        sstr_free(buf);
    }
    PreciseInts& operator=(const PreciseInts& other) {
        if (this != &other) {
            PreciseInts_clear(&data_);
            PreciseInts_init(&data_);
            sstr_t buf = sstr_new();
            json_marshal_indent_PreciseInts(const_cast<struct ::PreciseInts*>(&other.data_), 0, 0, buf);
            json_unmarshal_PreciseInts(buf, &data_);
            sstr_free(buf);
        }
        return *this;
    }

    // Field accessors
    int8_t i8() const { return data_.i8; }
    void set_i8(int8_t v) { data_.i8 = v; }
    int16_t i16() const { return data_.i16; }
    void set_i16(int16_t v) { data_.i16 = v; }
    int32_t i32() const { return data_.i32; }
    void set_i32(int32_t v) { data_.i32 = v; }
    int64_t i64() const { return data_.i64; }
    void set_i64(int64_t v) { data_.i64 = v; }
    uint8_t u8() const { return data_.u8; }
    void set_u8(uint8_t v) { data_.u8 = v; }
    uint16_t u16() const { return data_.u16; }
    void set_u16(uint16_t v) { data_.u16 = v; }
    uint32_t u32() const { return data_.u32; }
    void set_u32(uint32_t v) { data_.u32 = v; }
    uint64_t u64() const { return data_.u64; }
    void set_u64(uint64_t v) { data_.u64 = v; }

    /// Serialize to string.
    std::string marshal() const {
        sstr_t buf = sstr_new();
        json_marshal_indent_PreciseInts(const_cast<struct ::PreciseInts*>(&data_), 0, 0, buf);
        std::string result(sstr_cstr(buf), sstr_length(buf));
        sstr_free(buf);
        return result;
    }

    /// Deserialize from string. Throws on error.
    static PreciseInts unmarshal(const std::string& data) {
        PreciseInts obj;
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_PreciseInts(in, &obj.data_);
        sstr_free(in);
        if (rc != 0) throw std::runtime_error("unmarshal failed");
        return obj;
    }

    /// Deserialize into this object. Returns true on success.
    bool unmarshal_into(const std::string& data) {
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_PreciseInts(in, &data_);
        sstr_free(in);
        return rc == 0;
    }

    /// Access the underlying C struct.
    struct ::PreciseInts& c_struct() { return data_; }
    const struct ::PreciseInts& c_struct() const { return data_; }

    bool operator==(const PreciseInts& other) const { return marshal() == other.marshal(); }
    bool operator!=(const PreciseInts& other) const { return !(*this == other); }

private:
    struct ::PreciseInts data_;
};

class EdgeCaseStruct {
public:
    EdgeCaseStruct() { EdgeCaseStruct_init(&data_); }
    ~EdgeCaseStruct() { EdgeCaseStruct_clear(&data_); }

    EdgeCaseStruct(EdgeCaseStruct&& other) noexcept {
        std::memcpy(&data_, &other.data_, sizeof(data_));
        std::memset(&other.data_, 0, sizeof(other.data_));
    }
    EdgeCaseStruct& operator=(EdgeCaseStruct&& other) noexcept {
        if (this != &other) {
            EdgeCaseStruct_clear(&data_);
            std::memcpy(&data_, &other.data_, sizeof(data_));
            std::memset(&other.data_, 0, sizeof(other.data_));
        }
        return *this;
    }

    EdgeCaseStruct(const EdgeCaseStruct& other) {
        EdgeCaseStruct_init(&data_);
        sstr_t buf = sstr_new();
        json_marshal_indent_EdgeCaseStruct(const_cast<struct ::EdgeCaseStruct*>(&other.data_), 0, 0, buf);
        json_unmarshal_EdgeCaseStruct(buf, &data_);
        sstr_free(buf);
    }
    EdgeCaseStruct& operator=(const EdgeCaseStruct& other) {
        if (this != &other) {
            EdgeCaseStruct_clear(&data_);
            EdgeCaseStruct_init(&data_);
            sstr_t buf = sstr_new();
            json_marshal_indent_EdgeCaseStruct(const_cast<struct ::EdgeCaseStruct*>(&other.data_), 0, 0, buf);
            json_unmarshal_EdgeCaseStruct(buf, &data_);
            sstr_free(buf);
        }
        return *this;
    }

    // Field accessors
    int zero_int() const { return data_.zero_int; }
    void set_zero_int(int v) { data_.zero_int = v; }
    long negative_long() const { return data_.negative_long; }
    void set_negative_long(long v) { data_.negative_long = v; }
    float tiny_float() const { return data_.tiny_float; }
    void set_tiny_float(float v) { data_.tiny_float = v; }
    double huge_double() const { return data_.huge_double; }
    void set_huge_double(double v) { data_.huge_double = v; }
    bool false_bool() const { return static_cast<bool>(data_.false_bool); }
    void set_false_bool(bool v) { data_.false_bool = v; }
    std::string empty_string() const {
        if (!data_.empty_string) return std::string();
        return std::string(sstr_cstr(data_.empty_string), sstr_length(data_.empty_string));
    }
    void set_empty_string(const std::string& v) {
        if (!data_.empty_string) data_.empty_string = sstr_new();
        sstr_clear(data_.empty_string);
        sstr_append_of(data_.empty_string, v.data(), v.size());
    }
    void set_empty_string(const char* v) {
        if (!data_.empty_string) data_.empty_string = sstr_new();
        sstr_clear(data_.empty_string);
        sstr_append_cstr(data_.empty_string, v);
    }
    std::string special_chars_string() const {
        if (!data_.special_chars_string) return std::string();
        return std::string(sstr_cstr(data_.special_chars_string), sstr_length(data_.special_chars_string));
    }
    void set_special_chars_string(const std::string& v) {
        if (!data_.special_chars_string) data_.special_chars_string = sstr_new();
        sstr_clear(data_.special_chars_string);
        sstr_append_of(data_.special_chars_string, v.data(), v.size());
    }
    void set_special_chars_string(const char* v) {
        if (!data_.special_chars_string) data_.special_chars_string = sstr_new();
        sstr_clear(data_.special_chars_string);
        sstr_append_cstr(data_.special_chars_string, v);
    }

    /// Serialize to string.
    std::string marshal() const {
        sstr_t buf = sstr_new();
        json_marshal_indent_EdgeCaseStruct(const_cast<struct ::EdgeCaseStruct*>(&data_), 0, 0, buf);
        std::string result(sstr_cstr(buf), sstr_length(buf));
        sstr_free(buf);
        return result;
    }

    /// Deserialize from string. Throws on error.
    static EdgeCaseStruct unmarshal(const std::string& data) {
        EdgeCaseStruct obj;
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_EdgeCaseStruct(in, &obj.data_);
        sstr_free(in);
        if (rc != 0) throw std::runtime_error("unmarshal failed");
        return obj;
    }

    /// Deserialize into this object. Returns true on success.
    bool unmarshal_into(const std::string& data) {
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_EdgeCaseStruct(in, &data_);
        sstr_free(in);
        return rc == 0;
    }

    /// Access the underlying C struct.
    struct ::EdgeCaseStruct& c_struct() { return data_; }
    const struct ::EdgeCaseStruct& c_struct() const { return data_; }

    bool operator==(const EdgeCaseStruct& other) const { return marshal() == other.marshal(); }
    bool operator!=(const EdgeCaseStruct& other) const { return !(*this == other); }

private:
    struct ::EdgeCaseStruct data_;
};

class TestStruct {
public:
    TestStruct() { TestStruct_init(&data_); }
    ~TestStruct() { TestStruct_clear(&data_); }

    TestStruct(TestStruct&& other) noexcept {
        std::memcpy(&data_, &other.data_, sizeof(data_));
        std::memset(&other.data_, 0, sizeof(other.data_));
    }
    TestStruct& operator=(TestStruct&& other) noexcept {
        if (this != &other) {
            TestStruct_clear(&data_);
            std::memcpy(&data_, &other.data_, sizeof(data_));
            std::memset(&other.data_, 0, sizeof(other.data_));
        }
        return *this;
    }

    TestStruct(const TestStruct& other) {
        TestStruct_init(&data_);
        sstr_t buf = sstr_new();
        json_marshal_indent_TestStruct(const_cast<struct ::TestStruct*>(&other.data_), 0, 0, buf);
        json_unmarshal_TestStruct(buf, &data_);
        sstr_free(buf);
    }
    TestStruct& operator=(const TestStruct& other) {
        if (this != &other) {
            TestStruct_clear(&data_);
            TestStruct_init(&data_);
            sstr_t buf = sstr_new();
            json_marshal_indent_TestStruct(const_cast<struct ::TestStruct*>(&other.data_), 0, 0, buf);
            json_unmarshal_TestStruct(buf, &data_);
            sstr_free(buf);
        }
        return *this;
    }

    // Field accessors
    int int_val() const { return data_.int_val; }
    void set_int_val(int v) { data_.int_val = v; }
    long long_val() const { return data_.long_val; }
    void set_long_val(long v) { data_.long_val = v; }
    float float_val() const { return data_.float_val; }
    void set_float_val(float v) { data_.float_val = v; }
    double double_val() const { return data_.double_val; }
    void set_double_val(double v) { data_.double_val = v; }
    bool bool_val() const { return static_cast<bool>(data_.bool_val); }
    void set_bool_val(bool v) { data_.bool_val = v; }
    std::string sstr_val() const {
        if (!data_.sstr_val) return std::string();
        return std::string(sstr_cstr(data_.sstr_val), sstr_length(data_.sstr_val));
    }
    void set_sstr_val(const std::string& v) {
        if (!data_.sstr_val) data_.sstr_val = sstr_new();
        sstr_clear(data_.sstr_val);
        sstr_append_of(data_.sstr_val, v.data(), v.size());
    }
    void set_sstr_val(const char* v) {
        if (!data_.sstr_val) data_.sstr_val = sstr_new();
        sstr_clear(data_.sstr_val);
        sstr_append_cstr(data_.sstr_val, v);
    }

    /// Serialize to string.
    std::string marshal() const {
        sstr_t buf = sstr_new();
        json_marshal_indent_TestStruct(const_cast<struct ::TestStruct*>(&data_), 0, 0, buf);
        std::string result(sstr_cstr(buf), sstr_length(buf));
        sstr_free(buf);
        return result;
    }

    /// Deserialize from string. Throws on error.
    static TestStruct unmarshal(const std::string& data) {
        TestStruct obj;
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_TestStruct(in, &obj.data_);
        sstr_free(in);
        if (rc != 0) throw std::runtime_error("unmarshal failed");
        return obj;
    }

    /// Deserialize into this object. Returns true on success.
    bool unmarshal_into(const std::string& data) {
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_TestStruct(in, &data_);
        sstr_free(in);
        return rc == 0;
    }

    /// Access the underlying C struct.
    struct ::TestStruct& c_struct() { return data_; }
    const struct ::TestStruct& c_struct() const { return data_; }

    bool operator==(const TestStruct& other) const { return marshal() == other.marshal(); }
    bool operator!=(const TestStruct& other) const { return !(*this == other); }

private:
    struct ::TestStruct data_;
};

class OptionalOnlyStruct {
public:
    OptionalOnlyStruct() { OptionalOnlyStruct_init(&data_); }
    ~OptionalOnlyStruct() { OptionalOnlyStruct_clear(&data_); }

    OptionalOnlyStruct(OptionalOnlyStruct&& other) noexcept {
        std::memcpy(&data_, &other.data_, sizeof(data_));
        std::memset(&other.data_, 0, sizeof(other.data_));
    }
    OptionalOnlyStruct& operator=(OptionalOnlyStruct&& other) noexcept {
        if (this != &other) {
            OptionalOnlyStruct_clear(&data_);
            std::memcpy(&data_, &other.data_, sizeof(data_));
            std::memset(&other.data_, 0, sizeof(other.data_));
        }
        return *this;
    }

    OptionalOnlyStruct(const OptionalOnlyStruct& other) {
        OptionalOnlyStruct_init(&data_);
        sstr_t buf = sstr_new();
        json_marshal_indent_OptionalOnlyStruct(const_cast<struct ::OptionalOnlyStruct*>(&other.data_), 0, 0, buf);
        json_unmarshal_OptionalOnlyStruct(buf, &data_);
        sstr_free(buf);
    }
    OptionalOnlyStruct& operator=(const OptionalOnlyStruct& other) {
        if (this != &other) {
            OptionalOnlyStruct_clear(&data_);
            OptionalOnlyStruct_init(&data_);
            sstr_t buf = sstr_new();
            json_marshal_indent_OptionalOnlyStruct(const_cast<struct ::OptionalOnlyStruct*>(&other.data_), 0, 0, buf);
            json_unmarshal_OptionalOnlyStruct(buf, &data_);
            sstr_free(buf);
        }
        return *this;
    }

    // Field accessors
    int id() const { return data_.id; }
    void set_id(int v) { data_.id = v; }
    std::string name() const {
        if (!data_.name) return std::string();
        return std::string(sstr_cstr(data_.name), sstr_length(data_.name));
    }
    void set_name(const std::string& v) {
        if (!data_.name) data_.name = sstr_new();
        sstr_clear(data_.name);
        sstr_append_of(data_.name, v.data(), v.size());
    }
    void set_name(const char* v) {
        if (!data_.name) data_.name = sstr_new();
        sstr_clear(data_.name);
        sstr_append_cstr(data_.name, v);
    }
    bool has_name() const { return data_.has_name != 0; }
    void set_has_name(bool v) { data_.has_name = v; }
    int score() const { return data_.score; }
    void set_score(int v) { data_.score = v; }
    bool has_score() const { return data_.has_score != 0; }
    void set_has_score(bool v) { data_.has_score = v; }
    bool active() const { return static_cast<bool>(data_.active); }
    void set_active(bool v) { data_.active = v; }
    bool has_active() const { return data_.has_active != 0; }
    void set_has_active(bool v) { data_.has_active = v; }
    float rating() const { return data_.rating; }
    void set_rating(float v) { data_.rating = v; }
    bool has_rating() const { return data_.has_rating != 0; }
    void set_has_rating(bool v) { data_.has_rating = v; }
    double precise() const { return data_.precise; }
    void set_precise(double v) { data_.precise = v; }
    bool has_precise() const { return data_.has_precise != 0; }
    void set_has_precise(bool v) { data_.has_precise = v; }
    long big_num() const { return data_.big_num; }
    void set_big_num(long v) { data_.big_num = v; }
    bool has_big_num() const { return data_.has_big_num != 0; }
    void set_has_big_num(bool v) { data_.has_big_num = v; }

    /// Serialize to string.
    std::string marshal() const {
        sstr_t buf = sstr_new();
        json_marshal_indent_OptionalOnlyStruct(const_cast<struct ::OptionalOnlyStruct*>(&data_), 0, 0, buf);
        std::string result(sstr_cstr(buf), sstr_length(buf));
        sstr_free(buf);
        return result;
    }

    /// Deserialize from string. Throws on error.
    static OptionalOnlyStruct unmarshal(const std::string& data) {
        OptionalOnlyStruct obj;
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_OptionalOnlyStruct(in, &obj.data_);
        sstr_free(in);
        if (rc != 0) throw std::runtime_error("unmarshal failed");
        return obj;
    }

    /// Deserialize into this object. Returns true on success.
    bool unmarshal_into(const std::string& data) {
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_OptionalOnlyStruct(in, &data_);
        sstr_free(in);
        return rc == 0;
    }

    /// Access the underlying C struct.
    struct ::OptionalOnlyStruct& c_struct() { return data_; }
    const struct ::OptionalOnlyStruct& c_struct() const { return data_; }

    bool operator==(const OptionalOnlyStruct& other) const { return marshal() == other.marshal(); }
    bool operator!=(const OptionalOnlyStruct& other) const { return !(*this == other); }

private:
    struct ::OptionalOnlyStruct data_;
};

class MapIntStruct {
public:
    MapIntStruct() { MapIntStruct_init(&data_); }
    ~MapIntStruct() { MapIntStruct_clear(&data_); }

    MapIntStruct(MapIntStruct&& other) noexcept {
        std::memcpy(&data_, &other.data_, sizeof(data_));
        std::memset(&other.data_, 0, sizeof(other.data_));
    }
    MapIntStruct& operator=(MapIntStruct&& other) noexcept {
        if (this != &other) {
            MapIntStruct_clear(&data_);
            std::memcpy(&data_, &other.data_, sizeof(data_));
            std::memset(&other.data_, 0, sizeof(other.data_));
        }
        return *this;
    }

    MapIntStruct(const MapIntStruct& other) {
        MapIntStruct_init(&data_);
        sstr_t buf = sstr_new();
        json_marshal_indent_MapIntStruct(const_cast<struct ::MapIntStruct*>(&other.data_), 0, 0, buf);
        json_unmarshal_MapIntStruct(buf, &data_);
        sstr_free(buf);
    }
    MapIntStruct& operator=(const MapIntStruct& other) {
        if (this != &other) {
            MapIntStruct_clear(&data_);
            MapIntStruct_init(&data_);
            sstr_t buf = sstr_new();
            json_marshal_indent_MapIntStruct(const_cast<struct ::MapIntStruct*>(&other.data_), 0, 0, buf);
            json_unmarshal_MapIntStruct(buf, &data_);
            sstr_free(buf);
        }
        return *this;
    }

    // Field accessors
    struct ::json_map_int& scores() { return data_.scores; }
    const struct ::json_map_int& scores() const { return data_.scores; }

    /// Serialize to string.
    std::string marshal() const {
        sstr_t buf = sstr_new();
        json_marshal_indent_MapIntStruct(const_cast<struct ::MapIntStruct*>(&data_), 0, 0, buf);
        std::string result(sstr_cstr(buf), sstr_length(buf));
        sstr_free(buf);
        return result;
    }

    /// Deserialize from string. Throws on error.
    static MapIntStruct unmarshal(const std::string& data) {
        MapIntStruct obj;
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_MapIntStruct(in, &obj.data_);
        sstr_free(in);
        if (rc != 0) throw std::runtime_error("unmarshal failed");
        return obj;
    }

    /// Deserialize into this object. Returns true on success.
    bool unmarshal_into(const std::string& data) {
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_MapIntStruct(in, &data_);
        sstr_free(in);
        return rc == 0;
    }

    /// Access the underlying C struct.
    struct ::MapIntStruct& c_struct() { return data_; }
    const struct ::MapIntStruct& c_struct() const { return data_; }

    bool operator==(const MapIntStruct& other) const { return marshal() == other.marshal(); }
    bool operator!=(const MapIntStruct& other) const { return !(*this == other); }

private:
    struct ::MapIntStruct data_;
};

class Circle {
public:
    Circle() { Circle_init(&data_); }
    ~Circle() { Circle_clear(&data_); }

    Circle(Circle&& other) noexcept {
        std::memcpy(&data_, &other.data_, sizeof(data_));
        std::memset(&other.data_, 0, sizeof(other.data_));
    }
    Circle& operator=(Circle&& other) noexcept {
        if (this != &other) {
            Circle_clear(&data_);
            std::memcpy(&data_, &other.data_, sizeof(data_));
            std::memset(&other.data_, 0, sizeof(other.data_));
        }
        return *this;
    }

    Circle(const Circle& other) {
        Circle_init(&data_);
        sstr_t buf = sstr_new();
        json_marshal_indent_Circle(const_cast<struct ::Circle*>(&other.data_), 0, 0, buf);
        json_unmarshal_Circle(buf, &data_);
        sstr_free(buf);
    }
    Circle& operator=(const Circle& other) {
        if (this != &other) {
            Circle_clear(&data_);
            Circle_init(&data_);
            sstr_t buf = sstr_new();
            json_marshal_indent_Circle(const_cast<struct ::Circle*>(&other.data_), 0, 0, buf);
            json_unmarshal_Circle(buf, &data_);
            sstr_free(buf);
        }
        return *this;
    }

    // Field accessors
    float radius() const { return data_.radius; }
    void set_radius(float v) { data_.radius = v; }

    /// Serialize to string.
    std::string marshal() const {
        sstr_t buf = sstr_new();
        json_marshal_indent_Circle(const_cast<struct ::Circle*>(&data_), 0, 0, buf);
        std::string result(sstr_cstr(buf), sstr_length(buf));
        sstr_free(buf);
        return result;
    }

    /// Deserialize from string. Throws on error.
    static Circle unmarshal(const std::string& data) {
        Circle obj;
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_Circle(in, &obj.data_);
        sstr_free(in);
        if (rc != 0) throw std::runtime_error("unmarshal failed");
        return obj;
    }

    /// Deserialize into this object. Returns true on success.
    bool unmarshal_into(const std::string& data) {
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_Circle(in, &data_);
        sstr_free(in);
        return rc == 0;
    }

    /// Access the underlying C struct.
    struct ::Circle& c_struct() { return data_; }
    const struct ::Circle& c_struct() const { return data_; }

    bool operator==(const Circle& other) const { return marshal() == other.marshal(); }
    bool operator!=(const Circle& other) const { return !(*this == other); }

private:
    struct ::Circle data_;
};

class DefaultPrecise {
public:
    DefaultPrecise() { DefaultPrecise_init(&data_); }
    ~DefaultPrecise() { DefaultPrecise_clear(&data_); }

    DefaultPrecise(DefaultPrecise&& other) noexcept {
        std::memcpy(&data_, &other.data_, sizeof(data_));
        std::memset(&other.data_, 0, sizeof(other.data_));
    }
    DefaultPrecise& operator=(DefaultPrecise&& other) noexcept {
        if (this != &other) {
            DefaultPrecise_clear(&data_);
            std::memcpy(&data_, &other.data_, sizeof(data_));
            std::memset(&other.data_, 0, sizeof(other.data_));
        }
        return *this;
    }

    DefaultPrecise(const DefaultPrecise& other) {
        DefaultPrecise_init(&data_);
        sstr_t buf = sstr_new();
        json_marshal_indent_DefaultPrecise(const_cast<struct ::DefaultPrecise*>(&other.data_), 0, 0, buf);
        json_unmarshal_DefaultPrecise(buf, &data_);
        sstr_free(buf);
    }
    DefaultPrecise& operator=(const DefaultPrecise& other) {
        if (this != &other) {
            DefaultPrecise_clear(&data_);
            DefaultPrecise_init(&data_);
            sstr_t buf = sstr_new();
            json_marshal_indent_DefaultPrecise(const_cast<struct ::DefaultPrecise*>(&other.data_), 0, 0, buf);
            json_unmarshal_DefaultPrecise(buf, &data_);
            sstr_free(buf);
        }
        return *this;
    }

    // Field accessors
    int8_t i8() const { return data_.i8; }
    void set_i8(int8_t v) { data_.i8 = v; }
    int16_t i16() const { return data_.i16; }
    void set_i16(int16_t v) { data_.i16 = v; }
    int32_t i32() const { return data_.i32; }
    void set_i32(int32_t v) { data_.i32 = v; }
    uint8_t u8() const { return data_.u8; }
    void set_u8(uint8_t v) { data_.u8 = v; }
    uint16_t u16() const { return data_.u16; }
    void set_u16(uint16_t v) { data_.u16 = v; }
    uint32_t u32() const { return data_.u32; }
    void set_u32(uint32_t v) { data_.u32 = v; }

    /// Serialize to string.
    std::string marshal() const {
        sstr_t buf = sstr_new();
        json_marshal_indent_DefaultPrecise(const_cast<struct ::DefaultPrecise*>(&data_), 0, 0, buf);
        std::string result(sstr_cstr(buf), sstr_length(buf));
        sstr_free(buf);
        return result;
    }

    /// Deserialize from string. Throws on error.
    static DefaultPrecise unmarshal(const std::string& data) {
        DefaultPrecise obj;
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_DefaultPrecise(in, &obj.data_);
        sstr_free(in);
        if (rc != 0) throw std::runtime_error("unmarshal failed");
        return obj;
    }

    /// Deserialize into this object. Returns true on success.
    bool unmarshal_into(const std::string& data) {
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_DefaultPrecise(in, &data_);
        sstr_free(in);
        return rc == 0;
    }

    /// Access the underlying C struct.
    struct ::DefaultPrecise& c_struct() { return data_; }
    const struct ::DefaultPrecise& c_struct() const { return data_; }

    bool operator==(const DefaultPrecise& other) const { return marshal() == other.marshal(); }
    bool operator!=(const DefaultPrecise& other) const { return !(*this == other); }

private:
    struct ::DefaultPrecise data_;
};

class DefaultMixed {
public:
    DefaultMixed() { DefaultMixed_init(&data_); }
    ~DefaultMixed() { DefaultMixed_clear(&data_); }

    DefaultMixed(DefaultMixed&& other) noexcept {
        std::memcpy(&data_, &other.data_, sizeof(data_));
        std::memset(&other.data_, 0, sizeof(other.data_));
    }
    DefaultMixed& operator=(DefaultMixed&& other) noexcept {
        if (this != &other) {
            DefaultMixed_clear(&data_);
            std::memcpy(&data_, &other.data_, sizeof(data_));
            std::memset(&other.data_, 0, sizeof(other.data_));
        }
        return *this;
    }

    DefaultMixed(const DefaultMixed& other) {
        DefaultMixed_init(&data_);
        sstr_t buf = sstr_new();
        json_marshal_indent_DefaultMixed(const_cast<struct ::DefaultMixed*>(&other.data_), 0, 0, buf);
        json_unmarshal_DefaultMixed(buf, &data_);
        sstr_free(buf);
    }
    DefaultMixed& operator=(const DefaultMixed& other) {
        if (this != &other) {
            DefaultMixed_clear(&data_);
            DefaultMixed_init(&data_);
            sstr_t buf = sstr_new();
            json_marshal_indent_DefaultMixed(const_cast<struct ::DefaultMixed*>(&other.data_), 0, 0, buf);
            json_unmarshal_DefaultMixed(buf, &data_);
            sstr_free(buf);
        }
        return *this;
    }

    // Field accessors
    int count() const { return data_.count; }
    void set_count(int v) { data_.count = v; }
    std::string name() const {
        if (!data_.name) return std::string();
        return std::string(sstr_cstr(data_.name), sstr_length(data_.name));
    }
    void set_name(const std::string& v) {
        if (!data_.name) data_.name = sstr_new();
        sstr_clear(data_.name);
        sstr_append_of(data_.name, v.data(), v.size());
    }
    void set_name(const char* v) {
        if (!data_.name) data_.name = sstr_new();
        sstr_clear(data_.name);
        sstr_append_cstr(data_.name, v);
    }
    bool enabled() const { return static_cast<bool>(data_.enabled); }
    void set_enabled(bool v) { data_.enabled = v; }

    /// Serialize to string.
    std::string marshal() const {
        sstr_t buf = sstr_new();
        json_marshal_indent_DefaultMixed(const_cast<struct ::DefaultMixed*>(&data_), 0, 0, buf);
        std::string result(sstr_cstr(buf), sstr_length(buf));
        sstr_free(buf);
        return result;
    }

    /// Deserialize from string. Throws on error.
    static DefaultMixed unmarshal(const std::string& data) {
        DefaultMixed obj;
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_DefaultMixed(in, &obj.data_);
        sstr_free(in);
        if (rc != 0) throw std::runtime_error("unmarshal failed");
        return obj;
    }

    /// Deserialize into this object. Returns true on success.
    bool unmarshal_into(const std::string& data) {
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_DefaultMixed(in, &data_);
        sstr_free(in);
        return rc == 0;
    }

    /// Access the underlying C struct.
    struct ::DefaultMixed& c_struct() { return data_; }
    const struct ::DefaultMixed& c_struct() const { return data_; }

    bool operator==(const DefaultMixed& other) const { return marshal() == other.marshal(); }
    bool operator!=(const DefaultMixed& other) const { return !(*this == other); }

private:
    struct ::DefaultMixed data_;
};

class DefaultBasic {
public:
    DefaultBasic() { DefaultBasic_init(&data_); }
    ~DefaultBasic() { DefaultBasic_clear(&data_); }

    DefaultBasic(DefaultBasic&& other) noexcept {
        std::memcpy(&data_, &other.data_, sizeof(data_));
        std::memset(&other.data_, 0, sizeof(other.data_));
    }
    DefaultBasic& operator=(DefaultBasic&& other) noexcept {
        if (this != &other) {
            DefaultBasic_clear(&data_);
            std::memcpy(&data_, &other.data_, sizeof(data_));
            std::memset(&other.data_, 0, sizeof(other.data_));
        }
        return *this;
    }

    DefaultBasic(const DefaultBasic& other) {
        DefaultBasic_init(&data_);
        sstr_t buf = sstr_new();
        json_marshal_indent_DefaultBasic(const_cast<struct ::DefaultBasic*>(&other.data_), 0, 0, buf);
        json_unmarshal_DefaultBasic(buf, &data_);
        sstr_free(buf);
    }
    DefaultBasic& operator=(const DefaultBasic& other) {
        if (this != &other) {
            DefaultBasic_clear(&data_);
            DefaultBasic_init(&data_);
            sstr_t buf = sstr_new();
            json_marshal_indent_DefaultBasic(const_cast<struct ::DefaultBasic*>(&other.data_), 0, 0, buf);
            json_unmarshal_DefaultBasic(buf, &data_);
            sstr_free(buf);
        }
        return *this;
    }

    // Field accessors
    int count() const { return data_.count; }
    void set_count(int v) { data_.count = v; }
    long big() const { return data_.big; }
    void set_big(long v) { data_.big = v; }
    float ratio() const { return data_.ratio; }
    void set_ratio(float v) { data_.ratio = v; }
    double precise() const { return data_.precise; }
    void set_precise(double v) { data_.precise = v; }
    bool active() const { return static_cast<bool>(data_.active); }
    void set_active(bool v) { data_.active = v; }
    std::string label() const {
        if (!data_.label) return std::string();
        return std::string(sstr_cstr(data_.label), sstr_length(data_.label));
    }
    void set_label(const std::string& v) {
        if (!data_.label) data_.label = sstr_new();
        sstr_clear(data_.label);
        sstr_append_of(data_.label, v.data(), v.size());
    }
    void set_label(const char* v) {
        if (!data_.label) data_.label = sstr_new();
        sstr_clear(data_.label);
        sstr_append_cstr(data_.label, v);
    }
    int no_default() const { return data_.no_default; }
    void set_no_default(int v) { data_.no_default = v; }

    /// Serialize to string.
    std::string marshal() const {
        sstr_t buf = sstr_new();
        json_marshal_indent_DefaultBasic(const_cast<struct ::DefaultBasic*>(&data_), 0, 0, buf);
        std::string result(sstr_cstr(buf), sstr_length(buf));
        sstr_free(buf);
        return result;
    }

    /// Deserialize from string. Throws on error.
    static DefaultBasic unmarshal(const std::string& data) {
        DefaultBasic obj;
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_DefaultBasic(in, &obj.data_);
        sstr_free(in);
        if (rc != 0) throw std::runtime_error("unmarshal failed");
        return obj;
    }

    /// Deserialize into this object. Returns true on success.
    bool unmarshal_into(const std::string& data) {
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_DefaultBasic(in, &data_);
        sstr_free(in);
        return rc == 0;
    }

    /// Access the underlying C struct.
    struct ::DefaultBasic& c_struct() { return data_; }
    const struct ::DefaultBasic& c_struct() const { return data_; }

    bool operator==(const DefaultBasic& other) const { return marshal() == other.marshal(); }
    bool operator!=(const DefaultBasic& other) const { return !(*this == other); }

private:
    struct ::DefaultBasic data_;
};

class AliasOptional {
public:
    AliasOptional() { AliasOptional_init(&data_); }
    ~AliasOptional() { AliasOptional_clear(&data_); }

    AliasOptional(AliasOptional&& other) noexcept {
        std::memcpy(&data_, &other.data_, sizeof(data_));
        std::memset(&other.data_, 0, sizeof(other.data_));
    }
    AliasOptional& operator=(AliasOptional&& other) noexcept {
        if (this != &other) {
            AliasOptional_clear(&data_);
            std::memcpy(&data_, &other.data_, sizeof(data_));
            std::memset(&other.data_, 0, sizeof(other.data_));
        }
        return *this;
    }

    AliasOptional(const AliasOptional& other) {
        AliasOptional_init(&data_);
        sstr_t buf = sstr_new();
        json_marshal_indent_AliasOptional(const_cast<struct ::AliasOptional*>(&other.data_), 0, 0, buf);
        json_unmarshal_AliasOptional(buf, &data_);
        sstr_free(buf);
    }
    AliasOptional& operator=(const AliasOptional& other) {
        if (this != &other) {
            AliasOptional_clear(&data_);
            AliasOptional_init(&data_);
            sstr_t buf = sstr_new();
            json_marshal_indent_AliasOptional(const_cast<struct ::AliasOptional*>(&other.data_), 0, 0, buf);
            json_unmarshal_AliasOptional(buf, &data_);
            sstr_free(buf);
        }
        return *this;
    }

    // Field accessors
    std::string name() const {
        if (!data_.name) return std::string();
        return std::string(sstr_cstr(data_.name), sstr_length(data_.name));
    }
    void set_name(const std::string& v) {
        if (!data_.name) data_.name = sstr_new();
        sstr_clear(data_.name);
        sstr_append_of(data_.name, v.data(), v.size());
    }
    void set_name(const char* v) {
        if (!data_.name) data_.name = sstr_new();
        sstr_clear(data_.name);
        sstr_append_cstr(data_.name, v);
    }
    bool has_name() const { return data_.has_name != 0; }
    void set_has_name(bool v) { data_.has_name = v; }
    int age() const { return data_.age; }
    void set_age(int v) { data_.age = v; }
    bool has_age() const { return data_.has_age != 0; }

    /// Serialize to string.
    std::string marshal() const {
        sstr_t buf = sstr_new();
        json_marshal_indent_AliasOptional(const_cast<struct ::AliasOptional*>(&data_), 0, 0, buf);
        std::string result(sstr_cstr(buf), sstr_length(buf));
        sstr_free(buf);
        return result;
    }

    /// Deserialize from string. Throws on error.
    static AliasOptional unmarshal(const std::string& data) {
        AliasOptional obj;
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_AliasOptional(in, &obj.data_);
        sstr_free(in);
        if (rc != 0) throw std::runtime_error("unmarshal failed");
        return obj;
    }

    /// Deserialize into this object. Returns true on success.
    bool unmarshal_into(const std::string& data) {
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_AliasOptional(in, &data_);
        sstr_free(in);
        return rc == 0;
    }

    /// Access the underlying C struct.
    struct ::AliasOptional& c_struct() { return data_; }
    const struct ::AliasOptional& c_struct() const { return data_; }

    bool operator==(const AliasOptional& other) const { return marshal() == other.marshal(); }
    bool operator!=(const AliasOptional& other) const { return !(*this == other); }

private:
    struct ::AliasOptional data_;
};

class AliasMixed {
public:
    AliasMixed() { AliasMixed_init(&data_); }
    ~AliasMixed() { AliasMixed_clear(&data_); }

    AliasMixed(AliasMixed&& other) noexcept {
        std::memcpy(&data_, &other.data_, sizeof(data_));
        std::memset(&other.data_, 0, sizeof(other.data_));
    }
    AliasMixed& operator=(AliasMixed&& other) noexcept {
        if (this != &other) {
            AliasMixed_clear(&data_);
            std::memcpy(&data_, &other.data_, sizeof(data_));
            std::memset(&other.data_, 0, sizeof(other.data_));
        }
        return *this;
    }

    AliasMixed(const AliasMixed& other) {
        AliasMixed_init(&data_);
        sstr_t buf = sstr_new();
        json_marshal_indent_AliasMixed(const_cast<struct ::AliasMixed*>(&other.data_), 0, 0, buf);
        json_unmarshal_AliasMixed(buf, &data_);
        sstr_free(buf);
    }
    AliasMixed& operator=(const AliasMixed& other) {
        if (this != &other) {
            AliasMixed_clear(&data_);
            AliasMixed_init(&data_);
            sstr_t buf = sstr_new();
            json_marshal_indent_AliasMixed(const_cast<struct ::AliasMixed*>(&other.data_), 0, 0, buf);
            json_unmarshal_AliasMixed(buf, &data_);
            sstr_free(buf);
        }
        return *this;
    }

    // Field accessors
    std::string first() const {
        if (!data_.first) return std::string();
        return std::string(sstr_cstr(data_.first), sstr_length(data_.first));
    }
    void set_first(const std::string& v) {
        if (!data_.first) data_.first = sstr_new();
        sstr_clear(data_.first);
        sstr_append_of(data_.first, v.data(), v.size());
    }
    void set_first(const char* v) {
        if (!data_.first) data_.first = sstr_new();
        sstr_clear(data_.first);
        sstr_append_cstr(data_.first, v);
    }
    std::string last() const {
        if (!data_.last) return std::string();
        return std::string(sstr_cstr(data_.last), sstr_length(data_.last));
    }
    void set_last(const std::string& v) {
        if (!data_.last) data_.last = sstr_new();
        sstr_clear(data_.last);
        sstr_append_of(data_.last, v.data(), v.size());
    }
    void set_last(const char* v) {
        if (!data_.last) data_.last = sstr_new();
        sstr_clear(data_.last);
        sstr_append_cstr(data_.last, v);
    }
    bool active() const { return static_cast<bool>(data_.active); }
    void set_active(bool v) { data_.active = v; }

    /// Serialize to string.
    std::string marshal() const {
        sstr_t buf = sstr_new();
        json_marshal_indent_AliasMixed(const_cast<struct ::AliasMixed*>(&data_), 0, 0, buf);
        std::string result(sstr_cstr(buf), sstr_length(buf));
        sstr_free(buf);
        return result;
    }

    /// Deserialize from string. Throws on error.
    static AliasMixed unmarshal(const std::string& data) {
        AliasMixed obj;
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_AliasMixed(in, &obj.data_);
        sstr_free(in);
        if (rc != 0) throw std::runtime_error("unmarshal failed");
        return obj;
    }

    /// Deserialize into this object. Returns true on success.
    bool unmarshal_into(const std::string& data) {
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_AliasMixed(in, &data_);
        sstr_free(in);
        return rc == 0;
    }

    /// Access the underlying C struct.
    struct ::AliasMixed& c_struct() { return data_; }
    const struct ::AliasMixed& c_struct() const { return data_; }

    bool operator==(const AliasMixed& other) const { return marshal() == other.marshal(); }
    bool operator!=(const AliasMixed& other) const { return !(*this == other); }

private:
    struct ::AliasMixed data_;
};

class NullableOnlyStruct {
public:
    NullableOnlyStruct() { NullableOnlyStruct_init(&data_); }
    ~NullableOnlyStruct() { NullableOnlyStruct_clear(&data_); }

    NullableOnlyStruct(NullableOnlyStruct&& other) noexcept {
        std::memcpy(&data_, &other.data_, sizeof(data_));
        std::memset(&other.data_, 0, sizeof(other.data_));
    }
    NullableOnlyStruct& operator=(NullableOnlyStruct&& other) noexcept {
        if (this != &other) {
            NullableOnlyStruct_clear(&data_);
            std::memcpy(&data_, &other.data_, sizeof(data_));
            std::memset(&other.data_, 0, sizeof(other.data_));
        }
        return *this;
    }

    NullableOnlyStruct(const NullableOnlyStruct& other) {
        NullableOnlyStruct_init(&data_);
        sstr_t buf = sstr_new();
        json_marshal_indent_NullableOnlyStruct(const_cast<struct ::NullableOnlyStruct*>(&other.data_), 0, 0, buf);
        json_unmarshal_NullableOnlyStruct(buf, &data_);
        sstr_free(buf);
    }
    NullableOnlyStruct& operator=(const NullableOnlyStruct& other) {
        if (this != &other) {
            NullableOnlyStruct_clear(&data_);
            NullableOnlyStruct_init(&data_);
            sstr_t buf = sstr_new();
            json_marshal_indent_NullableOnlyStruct(const_cast<struct ::NullableOnlyStruct*>(&other.data_), 0, 0, buf);
            json_unmarshal_NullableOnlyStruct(buf, &data_);
            sstr_free(buf);
        }
        return *this;
    }

    // Field accessors
    int id() const { return data_.id; }
    void set_id(int v) { data_.id = v; }
    std::string name() const {
        if (!data_.name) return std::string();
        return std::string(sstr_cstr(data_.name), sstr_length(data_.name));
    }
    void set_name(const std::string& v) {
        if (!data_.name) data_.name = sstr_new();
        sstr_clear(data_.name);
        sstr_append_of(data_.name, v.data(), v.size());
    }
    void set_name(const char* v) {
        if (!data_.name) data_.name = sstr_new();
        sstr_clear(data_.name);
        sstr_append_cstr(data_.name, v);
    }
    bool has_name() const { return data_.has_name != 0; }
    int score() const { return data_.score; }
    void set_score(int v) { data_.score = v; }
    bool has_score() const { return data_.has_score != 0; }
    bool active() const { return static_cast<bool>(data_.active); }
    void set_active(bool v) { data_.active = v; }
    bool has_active() const { return data_.has_active != 0; }

    /// Serialize to string.
    std::string marshal() const {
        sstr_t buf = sstr_new();
        json_marshal_indent_NullableOnlyStruct(const_cast<struct ::NullableOnlyStruct*>(&data_), 0, 0, buf);
        std::string result(sstr_cstr(buf), sstr_length(buf));
        sstr_free(buf);
        return result;
    }

    /// Deserialize from string. Throws on error.
    static NullableOnlyStruct unmarshal(const std::string& data) {
        NullableOnlyStruct obj;
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_NullableOnlyStruct(in, &obj.data_);
        sstr_free(in);
        if (rc != 0) throw std::runtime_error("unmarshal failed");
        return obj;
    }

    /// Deserialize into this object. Returns true on success.
    bool unmarshal_into(const std::string& data) {
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_NullableOnlyStruct(in, &data_);
        sstr_free(in);
        return rc == 0;
    }

    /// Access the underlying C struct.
    struct ::NullableOnlyStruct& c_struct() { return data_; }
    const struct ::NullableOnlyStruct& c_struct() const { return data_; }

    bool operator==(const NullableOnlyStruct& other) const { return marshal() == other.marshal(); }
    bool operator!=(const NullableOnlyStruct& other) const { return !(*this == other); }

private:
    struct ::NullableOnlyStruct data_;
};

class NestedStruct {
public:
    NestedStruct() { NestedStruct_init(&data_); }
    ~NestedStruct() { NestedStruct_clear(&data_); }

    NestedStruct(NestedStruct&& other) noexcept {
        std::memcpy(&data_, &other.data_, sizeof(data_));
        std::memset(&other.data_, 0, sizeof(other.data_));
    }
    NestedStruct& operator=(NestedStruct&& other) noexcept {
        if (this != &other) {
            NestedStruct_clear(&data_);
            std::memcpy(&data_, &other.data_, sizeof(data_));
            std::memset(&other.data_, 0, sizeof(other.data_));
        }
        return *this;
    }

    NestedStruct(const NestedStruct& other) {
        NestedStruct_init(&data_);
        sstr_t buf = sstr_new();
        json_marshal_indent_NestedStruct(const_cast<struct ::NestedStruct*>(&other.data_), 0, 0, buf);
        json_unmarshal_NestedStruct(buf, &data_);
        sstr_free(buf);
    }
    NestedStruct& operator=(const NestedStruct& other) {
        if (this != &other) {
            NestedStruct_clear(&data_);
            NestedStruct_init(&data_);
            sstr_t buf = sstr_new();
            json_marshal_indent_NestedStruct(const_cast<struct ::NestedStruct*>(&other.data_), 0, 0, buf);
            json_unmarshal_NestedStruct(buf, &data_);
            sstr_free(buf);
        }
        return *this;
    }

    // Field accessors
    int id() const { return data_.id; }
    void set_id(int v) { data_.id = v; }
    std::string name() const {
        if (!data_.name) return std::string();
        return std::string(sstr_cstr(data_.name), sstr_length(data_.name));
    }
    void set_name(const std::string& v) {
        if (!data_.name) data_.name = sstr_new();
        sstr_clear(data_.name);
        sstr_append_of(data_.name, v.data(), v.size());
    }
    void set_name(const char* v) {
        if (!data_.name) data_.name = sstr_new();
        sstr_clear(data_.name);
        sstr_append_cstr(data_.name, v);
    }
    struct ::TestStruct& embedded() { return data_.embedded; }
    const struct ::TestStruct& embedded() const { return data_.embedded; }

    /// Serialize to string.
    std::string marshal() const {
        sstr_t buf = sstr_new();
        json_marshal_indent_NestedStruct(const_cast<struct ::NestedStruct*>(&data_), 0, 0, buf);
        std::string result(sstr_cstr(buf), sstr_length(buf));
        sstr_free(buf);
        return result;
    }

    /// Deserialize from string. Throws on error.
    static NestedStruct unmarshal(const std::string& data) {
        NestedStruct obj;
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_NestedStruct(in, &obj.data_);
        sstr_free(in);
        if (rc != 0) throw std::runtime_error("unmarshal failed");
        return obj;
    }

    /// Deserialize into this object. Returns true on success.
    bool unmarshal_into(const std::string& data) {
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_NestedStruct(in, &data_);
        sstr_free(in);
        return rc == 0;
    }

    /// Access the underlying C struct.
    struct ::NestedStruct& c_struct() { return data_; }
    const struct ::NestedStruct& c_struct() const { return data_; }

    bool operator==(const NestedStruct& other) const { return marshal() == other.marshal(); }
    bool operator!=(const NestedStruct& other) const { return !(*this == other); }

private:
    struct ::NestedStruct data_;
};

class PreciseIntArrays {
public:
    PreciseIntArrays() { PreciseIntArrays_init(&data_); }
    ~PreciseIntArrays() { PreciseIntArrays_clear(&data_); }

    PreciseIntArrays(PreciseIntArrays&& other) noexcept {
        std::memcpy(&data_, &other.data_, sizeof(data_));
        std::memset(&other.data_, 0, sizeof(other.data_));
    }
    PreciseIntArrays& operator=(PreciseIntArrays&& other) noexcept {
        if (this != &other) {
            PreciseIntArrays_clear(&data_);
            std::memcpy(&data_, &other.data_, sizeof(data_));
            std::memset(&other.data_, 0, sizeof(other.data_));
        }
        return *this;
    }

    PreciseIntArrays(const PreciseIntArrays& other) {
        PreciseIntArrays_init(&data_);
        sstr_t buf = sstr_new();
        json_marshal_indent_PreciseIntArrays(const_cast<struct ::PreciseIntArrays*>(&other.data_), 0, 0, buf);
        json_unmarshal_PreciseIntArrays(buf, &data_);
        sstr_free(buf);
    }
    PreciseIntArrays& operator=(const PreciseIntArrays& other) {
        if (this != &other) {
            PreciseIntArrays_clear(&data_);
            PreciseIntArrays_init(&data_);
            sstr_t buf = sstr_new();
            json_marshal_indent_PreciseIntArrays(const_cast<struct ::PreciseIntArrays*>(&other.data_), 0, 0, buf);
            json_unmarshal_PreciseIntArrays(buf, &data_);
            sstr_free(buf);
        }
        return *this;
    }

    // Field accessors
    int8_t* i8_arr_data() { return data_.i8_arr; }
    static constexpr int i8_arr_size() { return 4; }
    uint32_t* u32_dyn_data() { return data_.u32_dyn; }
    int u32_dyn_len() const { return data_.u32_dyn_len; }
    int64_t* i64_dyn_data() { return data_.i64_dyn; }
    int i64_dyn_len() const { return data_.i64_dyn_len; }
    uint64_t* u64_fixed_data() { return data_.u64_fixed; }
    static constexpr int u64_fixed_size() { return 2; }

    /// Serialize to string.
    std::string marshal() const {
        sstr_t buf = sstr_new();
        json_marshal_indent_PreciseIntArrays(const_cast<struct ::PreciseIntArrays*>(&data_), 0, 0, buf);
        std::string result(sstr_cstr(buf), sstr_length(buf));
        sstr_free(buf);
        return result;
    }

    /// Deserialize from string. Throws on error.
    static PreciseIntArrays unmarshal(const std::string& data) {
        PreciseIntArrays obj;
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_PreciseIntArrays(in, &obj.data_);
        sstr_free(in);
        if (rc != 0) throw std::runtime_error("unmarshal failed");
        return obj;
    }

    /// Deserialize into this object. Returns true on success.
    bool unmarshal_into(const std::string& data) {
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_PreciseIntArrays(in, &data_);
        sstr_free(in);
        return rc == 0;
    }

    /// Access the underlying C struct.
    struct ::PreciseIntArrays& c_struct() { return data_; }
    const struct ::PreciseIntArrays& c_struct() const { return data_; }

    bool operator==(const PreciseIntArrays& other) const { return marshal() == other.marshal(); }
    bool operator!=(const PreciseIntArrays& other) const { return !(*this == other); }

private:
    struct ::PreciseIntArrays data_;
};

class AliasArray {
public:
    AliasArray() { AliasArray_init(&data_); }
    ~AliasArray() { AliasArray_clear(&data_); }

    AliasArray(AliasArray&& other) noexcept {
        std::memcpy(&data_, &other.data_, sizeof(data_));
        std::memset(&other.data_, 0, sizeof(other.data_));
    }
    AliasArray& operator=(AliasArray&& other) noexcept {
        if (this != &other) {
            AliasArray_clear(&data_);
            std::memcpy(&data_, &other.data_, sizeof(data_));
            std::memset(&other.data_, 0, sizeof(other.data_));
        }
        return *this;
    }

    AliasArray(const AliasArray& other) {
        AliasArray_init(&data_);
        sstr_t buf = sstr_new();
        json_marshal_indent_AliasArray(const_cast<struct ::AliasArray*>(&other.data_), 0, 0, buf);
        json_unmarshal_AliasArray(buf, &data_);
        sstr_free(buf);
    }
    AliasArray& operator=(const AliasArray& other) {
        if (this != &other) {
            AliasArray_clear(&data_);
            AliasArray_init(&data_);
            sstr_t buf = sstr_new();
            json_marshal_indent_AliasArray(const_cast<struct ::AliasArray*>(&other.data_), 0, 0, buf);
            json_unmarshal_AliasArray(buf, &data_);
            sstr_free(buf);
        }
        return *this;
    }

    // Field accessors
    sstr_t* tags_data() { return data_.tags; }
    int tags_len() const { return data_.tags_len; }
    int* scores_data() { return data_.scores; }
    static constexpr int scores_size() { return 3; }

    /// Serialize to string.
    std::string marshal() const {
        sstr_t buf = sstr_new();
        json_marshal_indent_AliasArray(const_cast<struct ::AliasArray*>(&data_), 0, 0, buf);
        std::string result(sstr_cstr(buf), sstr_length(buf));
        sstr_free(buf);
        return result;
    }

    /// Deserialize from string. Throws on error.
    static AliasArray unmarshal(const std::string& data) {
        AliasArray obj;
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_AliasArray(in, &obj.data_);
        sstr_free(in);
        if (rc != 0) throw std::runtime_error("unmarshal failed");
        return obj;
    }

    /// Deserialize into this object. Returns true on success.
    bool unmarshal_into(const std::string& data) {
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_AliasArray(in, &data_);
        sstr_free(in);
        return rc == 0;
    }

    /// Access the underlying C struct.
    struct ::AliasArray& c_struct() { return data_; }
    const struct ::AliasArray& c_struct() const { return data_; }

    bool operator==(const AliasArray& other) const { return marshal() == other.marshal(); }
    bool operator!=(const AliasArray& other) const { return !(*this == other); }

private:
    struct ::AliasArray data_;
};

class Rectangle {
public:
    Rectangle() { Rectangle_init(&data_); }
    ~Rectangle() { Rectangle_clear(&data_); }

    Rectangle(Rectangle&& other) noexcept {
        std::memcpy(&data_, &other.data_, sizeof(data_));
        std::memset(&other.data_, 0, sizeof(other.data_));
    }
    Rectangle& operator=(Rectangle&& other) noexcept {
        if (this != &other) {
            Rectangle_clear(&data_);
            std::memcpy(&data_, &other.data_, sizeof(data_));
            std::memset(&other.data_, 0, sizeof(other.data_));
        }
        return *this;
    }

    Rectangle(const Rectangle& other) {
        Rectangle_init(&data_);
        sstr_t buf = sstr_new();
        json_marshal_indent_Rectangle(const_cast<struct ::Rectangle*>(&other.data_), 0, 0, buf);
        json_unmarshal_Rectangle(buf, &data_);
        sstr_free(buf);
    }
    Rectangle& operator=(const Rectangle& other) {
        if (this != &other) {
            Rectangle_clear(&data_);
            Rectangle_init(&data_);
            sstr_t buf = sstr_new();
            json_marshal_indent_Rectangle(const_cast<struct ::Rectangle*>(&other.data_), 0, 0, buf);
            json_unmarshal_Rectangle(buf, &data_);
            sstr_free(buf);
        }
        return *this;
    }

    // Field accessors
    float width() const { return data_.width; }
    void set_width(float v) { data_.width = v; }
    float height() const { return data_.height; }
    void set_height(float v) { data_.height = v; }

    /// Serialize to string.
    std::string marshal() const {
        sstr_t buf = sstr_new();
        json_marshal_indent_Rectangle(const_cast<struct ::Rectangle*>(&data_), 0, 0, buf);
        std::string result(sstr_cstr(buf), sstr_length(buf));
        sstr_free(buf);
        return result;
    }

    /// Deserialize from string. Throws on error.
    static Rectangle unmarshal(const std::string& data) {
        Rectangle obj;
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_Rectangle(in, &obj.data_);
        sstr_free(in);
        if (rc != 0) throw std::runtime_error("unmarshal failed");
        return obj;
    }

    /// Deserialize into this object. Returns true on success.
    bool unmarshal_into(const std::string& data) {
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_Rectangle(in, &data_);
        sstr_free(in);
        return rc == 0;
    }

    /// Access the underlying C struct.
    struct ::Rectangle& c_struct() { return data_; }
    const struct ::Rectangle& c_struct() const { return data_; }

    bool operator==(const Rectangle& other) const { return marshal() == other.marshal(); }
    bool operator!=(const Rectangle& other) const { return !(*this == other); }

private:
    struct ::Rectangle data_;
};

class OptionalFieldsStruct {
public:
    OptionalFieldsStruct() { OptionalFieldsStruct_init(&data_); }
    ~OptionalFieldsStruct() { OptionalFieldsStruct_clear(&data_); }

    OptionalFieldsStruct(OptionalFieldsStruct&& other) noexcept {
        std::memcpy(&data_, &other.data_, sizeof(data_));
        std::memset(&other.data_, 0, sizeof(other.data_));
    }
    OptionalFieldsStruct& operator=(OptionalFieldsStruct&& other) noexcept {
        if (this != &other) {
            OptionalFieldsStruct_clear(&data_);
            std::memcpy(&data_, &other.data_, sizeof(data_));
            std::memset(&other.data_, 0, sizeof(other.data_));
        }
        return *this;
    }

    OptionalFieldsStruct(const OptionalFieldsStruct& other) {
        OptionalFieldsStruct_init(&data_);
        sstr_t buf = sstr_new();
        json_marshal_indent_OptionalFieldsStruct(const_cast<struct ::OptionalFieldsStruct*>(&other.data_), 0, 0, buf);
        json_unmarshal_OptionalFieldsStruct(buf, &data_);
        sstr_free(buf);
    }
    OptionalFieldsStruct& operator=(const OptionalFieldsStruct& other) {
        if (this != &other) {
            OptionalFieldsStruct_clear(&data_);
            OptionalFieldsStruct_init(&data_);
            sstr_t buf = sstr_new();
            json_marshal_indent_OptionalFieldsStruct(const_cast<struct ::OptionalFieldsStruct*>(&other.data_), 0, 0, buf);
            json_unmarshal_OptionalFieldsStruct(buf, &data_);
            sstr_free(buf);
        }
        return *this;
    }

    // Field accessors
    int required_id() const { return data_.required_id; }
    void set_required_id(int v) { data_.required_id = v; }
    std::string optional_name() const {
        if (!data_.optional_name) return std::string();
        return std::string(sstr_cstr(data_.optional_name), sstr_length(data_.optional_name));
    }
    void set_optional_name(const std::string& v) {
        if (!data_.optional_name) data_.optional_name = sstr_new();
        sstr_clear(data_.optional_name);
        sstr_append_of(data_.optional_name, v.data(), v.size());
    }
    void set_optional_name(const char* v) {
        if (!data_.optional_name) data_.optional_name = sstr_new();
        sstr_clear(data_.optional_name);
        sstr_append_cstr(data_.optional_name, v);
    }
    float optional_value() const { return data_.optional_value; }
    void set_optional_value(float v) { data_.optional_value = v; }
    bool has_data() const { return static_cast<bool>(data_.has_data); }
    void set_has_data(bool v) { data_.has_data = v; }

    /// Serialize to string.
    std::string marshal() const {
        sstr_t buf = sstr_new();
        json_marshal_indent_OptionalFieldsStruct(const_cast<struct ::OptionalFieldsStruct*>(&data_), 0, 0, buf);
        std::string result(sstr_cstr(buf), sstr_length(buf));
        sstr_free(buf);
        return result;
    }

    /// Deserialize from string. Throws on error.
    static OptionalFieldsStruct unmarshal(const std::string& data) {
        OptionalFieldsStruct obj;
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_OptionalFieldsStruct(in, &obj.data_);
        sstr_free(in);
        if (rc != 0) throw std::runtime_error("unmarshal failed");
        return obj;
    }

    /// Deserialize into this object. Returns true on success.
    bool unmarshal_into(const std::string& data) {
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_OptionalFieldsStruct(in, &data_);
        sstr_free(in);
        return rc == 0;
    }

    /// Access the underlying C struct.
    struct ::OptionalFieldsStruct& c_struct() { return data_; }
    const struct ::OptionalFieldsStruct& c_struct() const { return data_; }

    bool operator==(const OptionalFieldsStruct& other) const { return marshal() == other.marshal(); }
    bool operator!=(const OptionalFieldsStruct& other) const { return !(*this == other); }

private:
    struct ::OptionalFieldsStruct data_;
};

class PreciseIntMap {
public:
    PreciseIntMap() { PreciseIntMap_init(&data_); }
    ~PreciseIntMap() { PreciseIntMap_clear(&data_); }

    PreciseIntMap(PreciseIntMap&& other) noexcept {
        std::memcpy(&data_, &other.data_, sizeof(data_));
        std::memset(&other.data_, 0, sizeof(other.data_));
    }
    PreciseIntMap& operator=(PreciseIntMap&& other) noexcept {
        if (this != &other) {
            PreciseIntMap_clear(&data_);
            std::memcpy(&data_, &other.data_, sizeof(data_));
            std::memset(&other.data_, 0, sizeof(other.data_));
        }
        return *this;
    }

    PreciseIntMap(const PreciseIntMap& other) {
        PreciseIntMap_init(&data_);
        sstr_t buf = sstr_new();
        json_marshal_indent_PreciseIntMap(const_cast<struct ::PreciseIntMap*>(&other.data_), 0, 0, buf);
        json_unmarshal_PreciseIntMap(buf, &data_);
        sstr_free(buf);
    }
    PreciseIntMap& operator=(const PreciseIntMap& other) {
        if (this != &other) {
            PreciseIntMap_clear(&data_);
            PreciseIntMap_init(&data_);
            sstr_t buf = sstr_new();
            json_marshal_indent_PreciseIntMap(const_cast<struct ::PreciseIntMap*>(&other.data_), 0, 0, buf);
            json_unmarshal_PreciseIntMap(buf, &data_);
            sstr_free(buf);
        }
        return *this;
    }

    // Field accessors
    struct ::json_map_int32_t& i32_map() { return data_.i32_map; }
    const struct ::json_map_int32_t& i32_map() const { return data_.i32_map; }
    struct ::json_map_uint64_t& u64_map() { return data_.u64_map; }
    const struct ::json_map_uint64_t& u64_map() const { return data_.u64_map; }

    /// Serialize to string.
    std::string marshal() const {
        sstr_t buf = sstr_new();
        json_marshal_indent_PreciseIntMap(const_cast<struct ::PreciseIntMap*>(&data_), 0, 0, buf);
        std::string result(sstr_cstr(buf), sstr_length(buf));
        sstr_free(buf);
        return result;
    }

    /// Deserialize from string. Throws on error.
    static PreciseIntMap unmarshal(const std::string& data) {
        PreciseIntMap obj;
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_PreciseIntMap(in, &obj.data_);
        sstr_free(in);
        if (rc != 0) throw std::runtime_error("unmarshal failed");
        return obj;
    }

    /// Deserialize into this object. Returns true on success.
    bool unmarshal_into(const std::string& data) {
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_PreciseIntMap(in, &data_);
        sstr_free(in);
        return rc == 0;
    }

    /// Access the underlying C struct.
    struct ::PreciseIntMap& c_struct() { return data_; }
    const struct ::PreciseIntMap& c_struct() const { return data_; }

    bool operator==(const PreciseIntMap& other) const { return marshal() == other.marshal(); }
    bool operator!=(const PreciseIntMap& other) const { return !(*this == other); }

private:
    struct ::PreciseIntMap data_;
};

class DefaultEnum {
public:
    DefaultEnum() { DefaultEnum_init(&data_); }
    ~DefaultEnum() { DefaultEnum_clear(&data_); }

    DefaultEnum(DefaultEnum&& other) noexcept {
        std::memcpy(&data_, &other.data_, sizeof(data_));
        std::memset(&other.data_, 0, sizeof(other.data_));
    }
    DefaultEnum& operator=(DefaultEnum&& other) noexcept {
        if (this != &other) {
            DefaultEnum_clear(&data_);
            std::memcpy(&data_, &other.data_, sizeof(data_));
            std::memset(&other.data_, 0, sizeof(other.data_));
        }
        return *this;
    }

    DefaultEnum(const DefaultEnum& other) {
        DefaultEnum_init(&data_);
        sstr_t buf = sstr_new();
        json_marshal_indent_DefaultEnum(const_cast<struct ::DefaultEnum*>(&other.data_), 0, 0, buf);
        json_unmarshal_DefaultEnum(buf, &data_);
        sstr_free(buf);
    }
    DefaultEnum& operator=(const DefaultEnum& other) {
        if (this != &other) {
            DefaultEnum_clear(&data_);
            DefaultEnum_init(&data_);
            sstr_t buf = sstr_new();
            json_marshal_indent_DefaultEnum(const_cast<struct ::DefaultEnum*>(&other.data_), 0, 0, buf);
            json_unmarshal_DefaultEnum(buf, &data_);
            sstr_free(buf);
        }
        return *this;
    }

    // Field accessors
    enum Color color() const { return static_cast<enum Color>(data_.color); }
    void set_color(enum Color v) { data_.color = v; }
    enum Status status() const { return static_cast<enum Status>(data_.status); }
    void set_status(enum Status v) { data_.status = v; }

    /// Serialize to string.
    std::string marshal() const {
        sstr_t buf = sstr_new();
        json_marshal_indent_DefaultEnum(const_cast<struct ::DefaultEnum*>(&data_), 0, 0, buf);
        std::string result(sstr_cstr(buf), sstr_length(buf));
        sstr_free(buf);
        return result;
    }

    /// Deserialize from string. Throws on error.
    static DefaultEnum unmarshal(const std::string& data) {
        DefaultEnum obj;
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_DefaultEnum(in, &obj.data_);
        sstr_free(in);
        if (rc != 0) throw std::runtime_error("unmarshal failed");
        return obj;
    }

    /// Deserialize into this object. Returns true on success.
    bool unmarshal_into(const std::string& data) {
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_DefaultEnum(in, &data_);
        sstr_free(in);
        return rc == 0;
    }

    /// Access the underlying C struct.
    struct ::DefaultEnum& c_struct() { return data_; }
    const struct ::DefaultEnum& c_struct() const { return data_; }

    bool operator==(const DefaultEnum& other) const { return marshal() == other.marshal(); }
    bool operator!=(const DefaultEnum& other) const { return !(*this == other); }

private:
    struct ::DefaultEnum data_;
};

class DefaultNegative {
public:
    DefaultNegative() { DefaultNegative_init(&data_); }
    ~DefaultNegative() { DefaultNegative_clear(&data_); }

    DefaultNegative(DefaultNegative&& other) noexcept {
        std::memcpy(&data_, &other.data_, sizeof(data_));
        std::memset(&other.data_, 0, sizeof(other.data_));
    }
    DefaultNegative& operator=(DefaultNegative&& other) noexcept {
        if (this != &other) {
            DefaultNegative_clear(&data_);
            std::memcpy(&data_, &other.data_, sizeof(data_));
            std::memset(&other.data_, 0, sizeof(other.data_));
        }
        return *this;
    }

    DefaultNegative(const DefaultNegative& other) {
        DefaultNegative_init(&data_);
        sstr_t buf = sstr_new();
        json_marshal_indent_DefaultNegative(const_cast<struct ::DefaultNegative*>(&other.data_), 0, 0, buf);
        json_unmarshal_DefaultNegative(buf, &data_);
        sstr_free(buf);
    }
    DefaultNegative& operator=(const DefaultNegative& other) {
        if (this != &other) {
            DefaultNegative_clear(&data_);
            DefaultNegative_init(&data_);
            sstr_t buf = sstr_new();
            json_marshal_indent_DefaultNegative(const_cast<struct ::DefaultNegative*>(&other.data_), 0, 0, buf);
            json_unmarshal_DefaultNegative(buf, &data_);
            sstr_free(buf);
        }
        return *this;
    }

    // Field accessors
    int neg_int() const { return data_.neg_int; }
    void set_neg_int(int v) { data_.neg_int = v; }
    long neg_long() const { return data_.neg_long; }
    void set_neg_long(long v) { data_.neg_long = v; }
    float neg_float() const { return data_.neg_float; }
    void set_neg_float(float v) { data_.neg_float = v; }
    double neg_double() const { return data_.neg_double; }
    void set_neg_double(double v) { data_.neg_double = v; }

    /// Serialize to string.
    std::string marshal() const {
        sstr_t buf = sstr_new();
        json_marshal_indent_DefaultNegative(const_cast<struct ::DefaultNegative*>(&data_), 0, 0, buf);
        std::string result(sstr_cstr(buf), sstr_length(buf));
        sstr_free(buf);
        return result;
    }

    /// Deserialize from string. Throws on error.
    static DefaultNegative unmarshal(const std::string& data) {
        DefaultNegative obj;
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_DefaultNegative(in, &obj.data_);
        sstr_free(in);
        if (rc != 0) throw std::runtime_error("unmarshal failed");
        return obj;
    }

    /// Deserialize into this object. Returns true on success.
    bool unmarshal_into(const std::string& data) {
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_DefaultNegative(in, &data_);
        sstr_free(in);
        return rc == 0;
    }

    /// Access the underlying C struct.
    struct ::DefaultNegative& c_struct() { return data_; }
    const struct ::DefaultNegative& c_struct() const { return data_; }

    bool operator==(const DefaultNegative& other) const { return marshal() == other.marshal(); }
    bool operator!=(const DefaultNegative& other) const { return !(*this == other); }

private:
    struct ::DefaultNegative data_;
};

class Person {
public:
    Person() { Person_init(&data_); }
    ~Person() { Person_clear(&data_); }

    Person(Person&& other) noexcept {
        std::memcpy(&data_, &other.data_, sizeof(data_));
        std::memset(&other.data_, 0, sizeof(other.data_));
    }
    Person& operator=(Person&& other) noexcept {
        if (this != &other) {
            Person_clear(&data_);
            std::memcpy(&data_, &other.data_, sizeof(data_));
            std::memset(&other.data_, 0, sizeof(other.data_));
        }
        return *this;
    }

    Person(const Person& other) {
        Person_init(&data_);
        sstr_t buf = sstr_new();
        json_marshal_indent_Person(const_cast<struct ::Person*>(&other.data_), 0, 0, buf);
        json_unmarshal_Person(buf, &data_);
        sstr_free(buf);
    }
    Person& operator=(const Person& other) {
        if (this != &other) {
            Person_clear(&data_);
            Person_init(&data_);
            sstr_t buf = sstr_new();
            json_marshal_indent_Person(const_cast<struct ::Person*>(&other.data_), 0, 0, buf);
            json_unmarshal_Person(buf, &data_);
            sstr_free(buf);
        }
        return *this;
    }

    // Field accessors
    std::string name() const {
        if (!data_.name) return std::string();
        return std::string(sstr_cstr(data_.name), sstr_length(data_.name));
    }
    void set_name(const std::string& v) {
        if (!data_.name) data_.name = sstr_new();
        sstr_clear(data_.name);
        sstr_append_of(data_.name, v.data(), v.size());
    }
    void set_name(const char* v) {
        if (!data_.name) data_.name = sstr_new();
        sstr_clear(data_.name);
        sstr_append_cstr(data_.name, v);
    }
    std::string age() const {
        if (!data_.age) return std::string();
        return std::string(sstr_cstr(data_.age), sstr_length(data_.age));
    }
    void set_age(const std::string& v) {
        if (!data_.age) data_.age = sstr_new();
        sstr_clear(data_.age);
        sstr_append_of(data_.age, v.data(), v.size());
    }
    void set_age(const char* v) {
        if (!data_.age) data_.age = sstr_new();
        sstr_clear(data_.age);
        sstr_append_cstr(data_.age, v);
    }

    /// Serialize to string.
    std::string marshal() const {
        sstr_t buf = sstr_new();
        json_marshal_indent_Person(const_cast<struct ::Person*>(&data_), 0, 0, buf);
        std::string result(sstr_cstr(buf), sstr_length(buf));
        sstr_free(buf);
        return result;
    }

    /// Deserialize from string. Throws on error.
    static Person unmarshal(const std::string& data) {
        Person obj;
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_Person(in, &obj.data_);
        sstr_free(in);
        if (rc != 0) throw std::runtime_error("unmarshal failed");
        return obj;
    }

    /// Deserialize into this object. Returns true on success.
    bool unmarshal_into(const std::string& data) {
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_Person(in, &data_);
        sstr_free(in);
        return rc == 0;
    }

    /// Access the underlying C struct.
    struct ::Person& c_struct() { return data_; }
    const struct ::Person& c_struct() const { return data_; }

    bool operator==(const Person& other) const { return marshal() == other.marshal(); }
    bool operator!=(const Person& other) const { return !(*this == other); }

private:
    struct ::Person data_;
};

class MapArrayStruct {
public:
    MapArrayStruct() { MapArrayStruct_init(&data_); }
    ~MapArrayStruct() { MapArrayStruct_clear(&data_); }

    MapArrayStruct(MapArrayStruct&& other) noexcept {
        std::memcpy(&data_, &other.data_, sizeof(data_));
        std::memset(&other.data_, 0, sizeof(other.data_));
    }
    MapArrayStruct& operator=(MapArrayStruct&& other) noexcept {
        if (this != &other) {
            MapArrayStruct_clear(&data_);
            std::memcpy(&data_, &other.data_, sizeof(data_));
            std::memset(&other.data_, 0, sizeof(other.data_));
        }
        return *this;
    }

    MapArrayStruct(const MapArrayStruct& other) {
        MapArrayStruct_init(&data_);
        sstr_t buf = sstr_new();
        json_marshal_indent_MapArrayStruct(const_cast<struct ::MapArrayStruct*>(&other.data_), 0, 0, buf);
        json_unmarshal_MapArrayStruct(buf, &data_);
        sstr_free(buf);
    }
    MapArrayStruct& operator=(const MapArrayStruct& other) {
        if (this != &other) {
            MapArrayStruct_clear(&data_);
            MapArrayStruct_init(&data_);
            sstr_t buf = sstr_new();
            json_marshal_indent_MapArrayStruct(const_cast<struct ::MapArrayStruct*>(&other.data_), 0, 0, buf);
            json_unmarshal_MapArrayStruct(buf, &data_);
            sstr_free(buf);
        }
        return *this;
    }

    // Field accessors
    void* tags_data() { return data_.tags; }
    int tags_len() const { return data_.tags_len; }

    /// Serialize to string.
    std::string marshal() const {
        sstr_t buf = sstr_new();
        json_marshal_indent_MapArrayStruct(const_cast<struct ::MapArrayStruct*>(&data_), 0, 0, buf);
        std::string result(sstr_cstr(buf), sstr_length(buf));
        sstr_free(buf);
        return result;
    }

    /// Deserialize from string. Throws on error.
    static MapArrayStruct unmarshal(const std::string& data) {
        MapArrayStruct obj;
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_MapArrayStruct(in, &obj.data_);
        sstr_free(in);
        if (rc != 0) throw std::runtime_error("unmarshal failed");
        return obj;
    }

    /// Deserialize into this object. Returns true on success.
    bool unmarshal_into(const std::string& data) {
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_MapArrayStruct(in, &data_);
        sstr_free(in);
        return rc == 0;
    }

    /// Access the underlying C struct.
    struct ::MapArrayStruct& c_struct() { return data_; }
    const struct ::MapArrayStruct& c_struct() const { return data_; }

    bool operator==(const MapArrayStruct& other) const { return marshal() == other.marshal(); }
    bool operator!=(const MapArrayStruct& other) const { return !(*this == other); }

private:
    struct ::MapArrayStruct data_;
};

class EnumTestStruct {
public:
    EnumTestStruct() { EnumTestStruct_init(&data_); }
    ~EnumTestStruct() { EnumTestStruct_clear(&data_); }

    EnumTestStruct(EnumTestStruct&& other) noexcept {
        std::memcpy(&data_, &other.data_, sizeof(data_));
        std::memset(&other.data_, 0, sizeof(other.data_));
    }
    EnumTestStruct& operator=(EnumTestStruct&& other) noexcept {
        if (this != &other) {
            EnumTestStruct_clear(&data_);
            std::memcpy(&data_, &other.data_, sizeof(data_));
            std::memset(&other.data_, 0, sizeof(other.data_));
        }
        return *this;
    }

    EnumTestStruct(const EnumTestStruct& other) {
        EnumTestStruct_init(&data_);
        sstr_t buf = sstr_new();
        json_marshal_indent_EnumTestStruct(const_cast<struct ::EnumTestStruct*>(&other.data_), 0, 0, buf);
        json_unmarshal_EnumTestStruct(buf, &data_);
        sstr_free(buf);
    }
    EnumTestStruct& operator=(const EnumTestStruct& other) {
        if (this != &other) {
            EnumTestStruct_clear(&data_);
            EnumTestStruct_init(&data_);
            sstr_t buf = sstr_new();
            json_marshal_indent_EnumTestStruct(const_cast<struct ::EnumTestStruct*>(&other.data_), 0, 0, buf);
            json_unmarshal_EnumTestStruct(buf, &data_);
            sstr_free(buf);
        }
        return *this;
    }

    // Field accessors
    enum Color color() const { return static_cast<enum Color>(data_.color); }
    void set_color(enum Color v) { data_.color = v; }
    enum Status status() const { return static_cast<enum Status>(data_.status); }
    void set_status(enum Status v) { data_.status = v; }
    int value() const { return data_.value; }
    void set_value(int v) { data_.value = v; }
    int* colors_data() { return data_.colors; }
    int colors_len() const { return data_.colors_len; }

    /// Serialize to string.
    std::string marshal() const {
        sstr_t buf = sstr_new();
        json_marshal_indent_EnumTestStruct(const_cast<struct ::EnumTestStruct*>(&data_), 0, 0, buf);
        std::string result(sstr_cstr(buf), sstr_length(buf));
        sstr_free(buf);
        return result;
    }

    /// Deserialize from string. Throws on error.
    static EnumTestStruct unmarshal(const std::string& data) {
        EnumTestStruct obj;
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_EnumTestStruct(in, &obj.data_);
        sstr_free(in);
        if (rc != 0) throw std::runtime_error("unmarshal failed");
        return obj;
    }

    /// Deserialize into this object. Returns true on success.
    bool unmarshal_into(const std::string& data) {
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_EnumTestStruct(in, &data_);
        sstr_free(in);
        return rc == 0;
    }

    /// Access the underlying C struct.
    struct ::EnumTestStruct& c_struct() { return data_; }
    const struct ::EnumTestStruct& c_struct() const { return data_; }

    bool operator==(const EnumTestStruct& other) const { return marshal() == other.marshal(); }
    bool operator!=(const EnumTestStruct& other) const { return !(*this == other); }

private:
    struct ::EnumTestStruct data_;
};

class House {
public:
    House() { House_init(&data_); }
    ~House() { House_clear(&data_); }

    House(House&& other) noexcept {
        std::memcpy(&data_, &other.data_, sizeof(data_));
        std::memset(&other.data_, 0, sizeof(other.data_));
    }
    House& operator=(House&& other) noexcept {
        if (this != &other) {
            House_clear(&data_);
            std::memcpy(&data_, &other.data_, sizeof(data_));
            std::memset(&other.data_, 0, sizeof(other.data_));
        }
        return *this;
    }

    House(const House& other) {
        House_init(&data_);
        sstr_t buf = sstr_new();
        json_marshal_indent_House(const_cast<struct ::House*>(&other.data_), 0, 0, buf);
        json_unmarshal_House(buf, &data_);
        sstr_free(buf);
    }
    House& operator=(const House& other) {
        if (this != &other) {
            House_clear(&data_);
            House_init(&data_);
            sstr_t buf = sstr_new();
            json_marshal_indent_House(const_cast<struct ::House*>(&other.data_), 0, 0, buf);
            json_unmarshal_House(buf, &data_);
            sstr_free(buf);
        }
        return *this;
    }

    // Field accessors
    std::string number() const {
        if (!data_.number) return std::string();
        return std::string(sstr_cstr(data_.number), sstr_length(data_.number));
    }
    void set_number(const std::string& v) {
        if (!data_.number) data_.number = sstr_new();
        sstr_clear(data_.number);
        sstr_append_of(data_.number, v.data(), v.size());
    }
    void set_number(const char* v) {
        if (!data_.number) data_.number = sstr_new();
        sstr_clear(data_.number);
        sstr_append_cstr(data_.number, v);
    }
    std::string street() const {
        if (!data_.street) return std::string();
        return std::string(sstr_cstr(data_.street), sstr_length(data_.street));
    }
    void set_street(const std::string& v) {
        if (!data_.street) data_.street = sstr_new();
        sstr_clear(data_.street);
        sstr_append_of(data_.street, v.data(), v.size());
    }
    void set_street(const char* v) {
        if (!data_.street) data_.street = sstr_new();
        sstr_clear(data_.street);
        sstr_append_cstr(data_.street, v);
    }

    /// Serialize to string.
    std::string marshal() const {
        sstr_t buf = sstr_new();
        json_marshal_indent_House(const_cast<struct ::House*>(&data_), 0, 0, buf);
        std::string result(sstr_cstr(buf), sstr_length(buf));
        sstr_free(buf);
        return result;
    }

    /// Deserialize from string. Throws on error.
    static House unmarshal(const std::string& data) {
        House obj;
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_House(in, &obj.data_);
        sstr_free(in);
        if (rc != 0) throw std::runtime_error("unmarshal failed");
        return obj;
    }

    /// Deserialize into this object. Returns true on success.
    bool unmarshal_into(const std::string& data) {
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_House(in, &data_);
        sstr_free(in);
        return rc == 0;
    }

    /// Access the underlying C struct.
    struct ::House& c_struct() { return data_; }
    const struct ::House& c_struct() const { return data_; }

    bool operator==(const House& other) const { return marshal() == other.marshal(); }
    bool operator!=(const House& other) const { return !(*this == other); }

private:
    struct ::House data_;
};

class OptionalNullableStruct {
public:
    OptionalNullableStruct() { OptionalNullableStruct_init(&data_); }
    ~OptionalNullableStruct() { OptionalNullableStruct_clear(&data_); }

    OptionalNullableStruct(OptionalNullableStruct&& other) noexcept {
        std::memcpy(&data_, &other.data_, sizeof(data_));
        std::memset(&other.data_, 0, sizeof(other.data_));
    }
    OptionalNullableStruct& operator=(OptionalNullableStruct&& other) noexcept {
        if (this != &other) {
            OptionalNullableStruct_clear(&data_);
            std::memcpy(&data_, &other.data_, sizeof(data_));
            std::memset(&other.data_, 0, sizeof(other.data_));
        }
        return *this;
    }

    OptionalNullableStruct(const OptionalNullableStruct& other) {
        OptionalNullableStruct_init(&data_);
        sstr_t buf = sstr_new();
        json_marshal_indent_OptionalNullableStruct(const_cast<struct ::OptionalNullableStruct*>(&other.data_), 0, 0, buf);
        json_unmarshal_OptionalNullableStruct(buf, &data_);
        sstr_free(buf);
    }
    OptionalNullableStruct& operator=(const OptionalNullableStruct& other) {
        if (this != &other) {
            OptionalNullableStruct_clear(&data_);
            OptionalNullableStruct_init(&data_);
            sstr_t buf = sstr_new();
            json_marshal_indent_OptionalNullableStruct(const_cast<struct ::OptionalNullableStruct*>(&other.data_), 0, 0, buf);
            json_unmarshal_OptionalNullableStruct(buf, &data_);
            sstr_free(buf);
        }
        return *this;
    }

    // Field accessors
    int id() const { return data_.id; }
    void set_id(int v) { data_.id = v; }
    std::string name() const {
        if (!data_.name) return std::string();
        return std::string(sstr_cstr(data_.name), sstr_length(data_.name));
    }
    void set_name(const std::string& v) {
        if (!data_.name) data_.name = sstr_new();
        sstr_clear(data_.name);
        sstr_append_of(data_.name, v.data(), v.size());
    }
    void set_name(const char* v) {
        if (!data_.name) data_.name = sstr_new();
        sstr_clear(data_.name);
        sstr_append_cstr(data_.name, v);
    }
    bool has_name() const { return data_.has_name != 0; }
    void set_has_name(bool v) { data_.has_name = v; }
    int score() const { return data_.score; }
    void set_score(int v) { data_.score = v; }
    bool has_score() const { return data_.has_score != 0; }
    void set_has_score(bool v) { data_.has_score = v; }

    /// Serialize to string.
    std::string marshal() const {
        sstr_t buf = sstr_new();
        json_marshal_indent_OptionalNullableStruct(const_cast<struct ::OptionalNullableStruct*>(&data_), 0, 0, buf);
        std::string result(sstr_cstr(buf), sstr_length(buf));
        sstr_free(buf);
        return result;
    }

    /// Deserialize from string. Throws on error.
    static OptionalNullableStruct unmarshal(const std::string& data) {
        OptionalNullableStruct obj;
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_OptionalNullableStruct(in, &obj.data_);
        sstr_free(in);
        if (rc != 0) throw std::runtime_error("unmarshal failed");
        return obj;
    }

    /// Deserialize into this object. Returns true on success.
    bool unmarshal_into(const std::string& data) {
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_OptionalNullableStruct(in, &data_);
        sstr_free(in);
        return rc == 0;
    }

    /// Access the underlying C struct.
    struct ::OptionalNullableStruct& c_struct() { return data_; }
    const struct ::OptionalNullableStruct& c_struct() const { return data_; }

    bool operator==(const OptionalNullableStruct& other) const { return marshal() == other.marshal(); }
    bool operator!=(const OptionalNullableStruct& other) const { return !(*this == other); }

private:
    struct ::OptionalNullableStruct data_;
};

class DefaultOptional {
public:
    DefaultOptional() { DefaultOptional_init(&data_); }
    ~DefaultOptional() { DefaultOptional_clear(&data_); }

    DefaultOptional(DefaultOptional&& other) noexcept {
        std::memcpy(&data_, &other.data_, sizeof(data_));
        std::memset(&other.data_, 0, sizeof(other.data_));
    }
    DefaultOptional& operator=(DefaultOptional&& other) noexcept {
        if (this != &other) {
            DefaultOptional_clear(&data_);
            std::memcpy(&data_, &other.data_, sizeof(data_));
            std::memset(&other.data_, 0, sizeof(other.data_));
        }
        return *this;
    }

    DefaultOptional(const DefaultOptional& other) {
        DefaultOptional_init(&data_);
        sstr_t buf = sstr_new();
        json_marshal_indent_DefaultOptional(const_cast<struct ::DefaultOptional*>(&other.data_), 0, 0, buf);
        json_unmarshal_DefaultOptional(buf, &data_);
        sstr_free(buf);
    }
    DefaultOptional& operator=(const DefaultOptional& other) {
        if (this != &other) {
            DefaultOptional_clear(&data_);
            DefaultOptional_init(&data_);
            sstr_t buf = sstr_new();
            json_marshal_indent_DefaultOptional(const_cast<struct ::DefaultOptional*>(&other.data_), 0, 0, buf);
            json_unmarshal_DefaultOptional(buf, &data_);
            sstr_free(buf);
        }
        return *this;
    }

    // Field accessors
    int score() const { return data_.score; }
    void set_score(int v) { data_.score = v; }
    bool has_score() const { return data_.has_score != 0; }
    void set_has_score(bool v) { data_.has_score = v; }
    std::string name() const {
        if (!data_.name) return std::string();
        return std::string(sstr_cstr(data_.name), sstr_length(data_.name));
    }
    void set_name(const std::string& v) {
        if (!data_.name) data_.name = sstr_new();
        sstr_clear(data_.name);
        sstr_append_of(data_.name, v.data(), v.size());
    }
    void set_name(const char* v) {
        if (!data_.name) data_.name = sstr_new();
        sstr_clear(data_.name);
        sstr_append_cstr(data_.name, v);
    }
    bool has_name() const { return data_.has_name != 0; }
    void set_has_name(bool v) { data_.has_name = v; }

    /// Serialize to string.
    std::string marshal() const {
        sstr_t buf = sstr_new();
        json_marshal_indent_DefaultOptional(const_cast<struct ::DefaultOptional*>(&data_), 0, 0, buf);
        std::string result(sstr_cstr(buf), sstr_length(buf));
        sstr_free(buf);
        return result;
    }

    /// Deserialize from string. Throws on error.
    static DefaultOptional unmarshal(const std::string& data) {
        DefaultOptional obj;
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_DefaultOptional(in, &obj.data_);
        sstr_free(in);
        if (rc != 0) throw std::runtime_error("unmarshal failed");
        return obj;
    }

    /// Deserialize into this object. Returns true on success.
    bool unmarshal_into(const std::string& data) {
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_DefaultOptional(in, &data_);
        sstr_free(in);
        return rc == 0;
    }

    /// Access the underlying C struct.
    struct ::DefaultOptional& c_struct() { return data_; }
    const struct ::DefaultOptional& c_struct() const { return data_; }

    bool operator==(const DefaultOptional& other) const { return marshal() == other.marshal(); }
    bool operator!=(const DefaultOptional& other) const { return !(*this == other); }

private:
    struct ::DefaultOptional data_;
};

class ComplexStruct {
public:
    ComplexStruct() { ComplexStruct_init(&data_); }
    ~ComplexStruct() { ComplexStruct_clear(&data_); }

    ComplexStruct(ComplexStruct&& other) noexcept {
        std::memcpy(&data_, &other.data_, sizeof(data_));
        std::memset(&other.data_, 0, sizeof(other.data_));
    }
    ComplexStruct& operator=(ComplexStruct&& other) noexcept {
        if (this != &other) {
            ComplexStruct_clear(&data_);
            std::memcpy(&data_, &other.data_, sizeof(data_));
            std::memset(&other.data_, 0, sizeof(other.data_));
        }
        return *this;
    }

    ComplexStruct(const ComplexStruct& other) {
        ComplexStruct_init(&data_);
        sstr_t buf = sstr_new();
        json_marshal_indent_ComplexStruct(const_cast<struct ::ComplexStruct*>(&other.data_), 0, 0, buf);
        json_unmarshal_ComplexStruct(buf, &data_);
        sstr_free(buf);
    }
    ComplexStruct& operator=(const ComplexStruct& other) {
        if (this != &other) {
            ComplexStruct_clear(&data_);
            ComplexStruct_init(&data_);
            sstr_t buf = sstr_new();
            json_marshal_indent_ComplexStruct(const_cast<struct ::ComplexStruct*>(&other.data_), 0, 0, buf);
            json_unmarshal_ComplexStruct(buf, &data_);
            sstr_free(buf);
        }
        return *this;
    }

    // Field accessors
    int simple_int() const { return data_.simple_int; }
    void set_simple_int(int v) { data_.simple_int = v; }
    long simple_long() const { return data_.simple_long; }
    void set_simple_long(long v) { data_.simple_long = v; }
    float simple_float() const { return data_.simple_float; }
    void set_simple_float(float v) { data_.simple_float = v; }
    double simple_double() const { return data_.simple_double; }
    void set_simple_double(double v) { data_.simple_double = v; }
    bool simple_bool() const { return static_cast<bool>(data_.simple_bool); }
    void set_simple_bool(bool v) { data_.simple_bool = v; }
    std::string simple_string() const {
        if (!data_.simple_string) return std::string();
        return std::string(sstr_cstr(data_.simple_string), sstr_length(data_.simple_string));
    }
    void set_simple_string(const std::string& v) {
        if (!data_.simple_string) data_.simple_string = sstr_new();
        sstr_clear(data_.simple_string);
        sstr_append_of(data_.simple_string, v.data(), v.size());
    }
    void set_simple_string(const char* v) {
        if (!data_.simple_string) data_.simple_string = sstr_new();
        sstr_clear(data_.simple_string);
        sstr_append_cstr(data_.simple_string, v);
    }
    int* int_array_data() { return data_.int_array; }
    int int_array_len() const { return data_.int_array_len; }
    long* long_array_data() { return data_.long_array; }
    int long_array_len() const { return data_.long_array_len; }
    float* float_array_data() { return data_.float_array; }
    int float_array_len() const { return data_.float_array_len; }
    double* double_array_data() { return data_.double_array; }
    int double_array_len() const { return data_.double_array_len; }
    sstr_t* string_array_data() { return data_.string_array; }
    int string_array_len() const { return data_.string_array_len; }
    struct ::House& address() { return data_.address; }
    const struct ::House& address() const { return data_.address; }
    struct ::Person* contacts_data() { return data_.contacts; }
    int contacts_len() const { return data_.contacts_len; }

    /// Serialize to string.
    std::string marshal() const {
        sstr_t buf = sstr_new();
        json_marshal_indent_ComplexStruct(const_cast<struct ::ComplexStruct*>(&data_), 0, 0, buf);
        std::string result(sstr_cstr(buf), sstr_length(buf));
        sstr_free(buf);
        return result;
    }

    /// Deserialize from string. Throws on error.
    static ComplexStruct unmarshal(const std::string& data) {
        ComplexStruct obj;
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_ComplexStruct(in, &obj.data_);
        sstr_free(in);
        if (rc != 0) throw std::runtime_error("unmarshal failed");
        return obj;
    }

    /// Deserialize into this object. Returns true on success.
    bool unmarshal_into(const std::string& data) {
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_ComplexStruct(in, &data_);
        sstr_free(in);
        return rc == 0;
    }

    /// Access the underlying C struct.
    struct ::ComplexStruct& c_struct() { return data_; }
    const struct ::ComplexStruct& c_struct() const { return data_; }

    bool operator==(const ComplexStruct& other) const { return marshal() == other.marshal(); }
    bool operator!=(const ComplexStruct& other) const { return !(*this == other); }

private:
    struct ::ComplexStruct data_;
};

class MapAllTypesStruct {
public:
    MapAllTypesStruct() { MapAllTypesStruct_init(&data_); }
    ~MapAllTypesStruct() { MapAllTypesStruct_clear(&data_); }

    MapAllTypesStruct(MapAllTypesStruct&& other) noexcept {
        std::memcpy(&data_, &other.data_, sizeof(data_));
        std::memset(&other.data_, 0, sizeof(other.data_));
    }
    MapAllTypesStruct& operator=(MapAllTypesStruct&& other) noexcept {
        if (this != &other) {
            MapAllTypesStruct_clear(&data_);
            std::memcpy(&data_, &other.data_, sizeof(data_));
            std::memset(&other.data_, 0, sizeof(other.data_));
        }
        return *this;
    }

    MapAllTypesStruct(const MapAllTypesStruct& other) {
        MapAllTypesStruct_init(&data_);
        sstr_t buf = sstr_new();
        json_marshal_indent_MapAllTypesStruct(const_cast<struct ::MapAllTypesStruct*>(&other.data_), 0, 0, buf);
        json_unmarshal_MapAllTypesStruct(buf, &data_);
        sstr_free(buf);
    }
    MapAllTypesStruct& operator=(const MapAllTypesStruct& other) {
        if (this != &other) {
            MapAllTypesStruct_clear(&data_);
            MapAllTypesStruct_init(&data_);
            sstr_t buf = sstr_new();
            json_marshal_indent_MapAllTypesStruct(const_cast<struct ::MapAllTypesStruct*>(&other.data_), 0, 0, buf);
            json_unmarshal_MapAllTypesStruct(buf, &data_);
            sstr_free(buf);
        }
        return *this;
    }

    // Field accessors
    struct ::json_map_int& int_map() { return data_.int_map; }
    const struct ::json_map_int& int_map() const { return data_.int_map; }
    struct ::json_map_long& long_map() { return data_.long_map; }
    const struct ::json_map_long& long_map() const { return data_.long_map; }
    struct ::json_map_float& float_map() { return data_.float_map; }
    const struct ::json_map_float& float_map() const { return data_.float_map; }
    struct ::json_map_double& double_map() { return data_.double_map; }
    const struct ::json_map_double& double_map() const { return data_.double_map; }
    struct ::json_map_int& bool_map() { return data_.bool_map; }
    const struct ::json_map_int& bool_map() const { return data_.bool_map; }
    struct ::json_map_sstr_t& str_map() { return data_.str_map; }
    const struct ::json_map_sstr_t& str_map() const { return data_.str_map; }
    struct ::json_map_int& enum_map() { return data_.enum_map; }
    const struct ::json_map_int& enum_map() const { return data_.enum_map; }
    struct ::json_map_Person& struct_map() { return data_.struct_map; }
    const struct ::json_map_Person& struct_map() const { return data_.struct_map; }

    /// Serialize to string.
    std::string marshal() const {
        sstr_t buf = sstr_new();
        json_marshal_indent_MapAllTypesStruct(const_cast<struct ::MapAllTypesStruct*>(&data_), 0, 0, buf);
        std::string result(sstr_cstr(buf), sstr_length(buf));
        sstr_free(buf);
        return result;
    }

    /// Deserialize from string. Throws on error.
    static MapAllTypesStruct unmarshal(const std::string& data) {
        MapAllTypesStruct obj;
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_MapAllTypesStruct(in, &obj.data_);
        sstr_free(in);
        if (rc != 0) throw std::runtime_error("unmarshal failed");
        return obj;
    }

    /// Deserialize into this object. Returns true on success.
    bool unmarshal_into(const std::string& data) {
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_MapAllTypesStruct(in, &data_);
        sstr_free(in);
        return rc == 0;
    }

    /// Access the underlying C struct.
    struct ::MapAllTypesStruct& c_struct() { return data_; }
    const struct ::MapAllTypesStruct& c_struct() const { return data_; }

    bool operator==(const MapAllTypesStruct& other) const { return marshal() == other.marshal(); }
    bool operator!=(const MapAllTypesStruct& other) const { return !(*this == other); }

private:
    struct ::MapAllTypesStruct data_;
};

class FixedArrayStruct {
public:
    FixedArrayStruct() { FixedArrayStruct_init(&data_); }
    ~FixedArrayStruct() { FixedArrayStruct_clear(&data_); }

    FixedArrayStruct(FixedArrayStruct&& other) noexcept {
        std::memcpy(&data_, &other.data_, sizeof(data_));
        std::memset(&other.data_, 0, sizeof(other.data_));
    }
    FixedArrayStruct& operator=(FixedArrayStruct&& other) noexcept {
        if (this != &other) {
            FixedArrayStruct_clear(&data_);
            std::memcpy(&data_, &other.data_, sizeof(data_));
            std::memset(&other.data_, 0, sizeof(other.data_));
        }
        return *this;
    }

    FixedArrayStruct(const FixedArrayStruct& other) {
        FixedArrayStruct_init(&data_);
        sstr_t buf = sstr_new();
        json_marshal_indent_FixedArrayStruct(const_cast<struct ::FixedArrayStruct*>(&other.data_), 0, 0, buf);
        json_unmarshal_FixedArrayStruct(buf, &data_);
        sstr_free(buf);
    }
    FixedArrayStruct& operator=(const FixedArrayStruct& other) {
        if (this != &other) {
            FixedArrayStruct_clear(&data_);
            FixedArrayStruct_init(&data_);
            sstr_t buf = sstr_new();
            json_marshal_indent_FixedArrayStruct(const_cast<struct ::FixedArrayStruct*>(&other.data_), 0, 0, buf);
            json_unmarshal_FixedArrayStruct(buf, &data_);
            sstr_free(buf);
        }
        return *this;
    }

    // Field accessors
    int* fixed_ints_data() { return data_.fixed_ints; }
    static constexpr int fixed_ints_size() { return 5; }
    long* fixed_longs_data() { return data_.fixed_longs; }
    static constexpr int fixed_longs_size() { return 3; }
    float* fixed_floats_data() { return data_.fixed_floats; }
    static constexpr int fixed_floats_size() { return 4; }
    double* fixed_doubles_data() { return data_.fixed_doubles; }
    static constexpr int fixed_doubles_size() { return 3; }
    sstr_t* fixed_strings_data() { return data_.fixed_strings; }
    static constexpr int fixed_strings_size() { return 3; }
    int* fixed_bools_data() { return data_.fixed_bools; }
    static constexpr int fixed_bools_size() { return 2; }
    int* fixed_colors_data() { return data_.fixed_colors; }
    static constexpr int fixed_colors_size() { return 3; }
    struct ::Person* fixed_contacts_data() { return data_.fixed_contacts; }
    static constexpr int fixed_contacts_size() { return 2; }

    /// Serialize to string.
    std::string marshal() const {
        sstr_t buf = sstr_new();
        json_marshal_indent_FixedArrayStruct(const_cast<struct ::FixedArrayStruct*>(&data_), 0, 0, buf);
        std::string result(sstr_cstr(buf), sstr_length(buf));
        sstr_free(buf);
        return result;
    }

    /// Deserialize from string. Throws on error.
    static FixedArrayStruct unmarshal(const std::string& data) {
        FixedArrayStruct obj;
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_FixedArrayStruct(in, &obj.data_);
        sstr_free(in);
        if (rc != 0) throw std::runtime_error("unmarshal failed");
        return obj;
    }

    /// Deserialize into this object. Returns true on success.
    bool unmarshal_into(const std::string& data) {
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_FixedArrayStruct(in, &data_);
        sstr_free(in);
        return rc == 0;
    }

    /// Access the underlying C struct.
    struct ::FixedArrayStruct& c_struct() { return data_; }
    const struct ::FixedArrayStruct& c_struct() const { return data_; }

    bool operator==(const FixedArrayStruct& other) const { return marshal() == other.marshal(); }
    bool operator!=(const FixedArrayStruct& other) const { return !(*this == other); }

private:
    struct ::FixedArrayStruct data_;
};

class Drawing {
public:
    Drawing() { Drawing_init(&data_); }
    ~Drawing() { Drawing_clear(&data_); }

    Drawing(Drawing&& other) noexcept {
        std::memcpy(&data_, &other.data_, sizeof(data_));
        std::memset(&other.data_, 0, sizeof(other.data_));
    }
    Drawing& operator=(Drawing&& other) noexcept {
        if (this != &other) {
            Drawing_clear(&data_);
            std::memcpy(&data_, &other.data_, sizeof(data_));
            std::memset(&other.data_, 0, sizeof(other.data_));
        }
        return *this;
    }

    Drawing(const Drawing& other) {
        Drawing_init(&data_);
        sstr_t buf = sstr_new();
        json_marshal_indent_Drawing(const_cast<struct ::Drawing*>(&other.data_), 0, 0, buf);
        json_unmarshal_Drawing(buf, &data_);
        sstr_free(buf);
    }
    Drawing& operator=(const Drawing& other) {
        if (this != &other) {
            Drawing_clear(&data_);
            Drawing_init(&data_);
            sstr_t buf = sstr_new();
            json_marshal_indent_Drawing(const_cast<struct ::Drawing*>(&other.data_), 0, 0, buf);
            json_unmarshal_Drawing(buf, &data_);
            sstr_free(buf);
        }
        return *this;
    }

    // Field accessors
    std::string name() const {
        if (!data_.name) return std::string();
        return std::string(sstr_cstr(data_.name), sstr_length(data_.name));
    }
    void set_name(const std::string& v) {
        if (!data_.name) data_.name = sstr_new();
        sstr_clear(data_.name);
        sstr_append_of(data_.name, v.data(), v.size());
    }
    void set_name(const char* v) {
        if (!data_.name) data_.name = sstr_new();
        sstr_clear(data_.name);
        sstr_append_cstr(data_.name, v);
    }
    struct ::Shape& shape() { return data_.shape; }
    const struct ::Shape& shape() const { return data_.shape; }
    void* shapes_data() { return data_.shapes; }
    int shapes_len() const { return data_.shapes_len; }

    /// Serialize to string.
    std::string marshal() const {
        sstr_t buf = sstr_new();
        json_marshal_indent_Drawing(const_cast<struct ::Drawing*>(&data_), 0, 0, buf);
        std::string result(sstr_cstr(buf), sstr_length(buf));
        sstr_free(buf);
        return result;
    }

    /// Deserialize from string. Throws on error.
    static Drawing unmarshal(const std::string& data) {
        Drawing obj;
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_Drawing(in, &obj.data_);
        sstr_free(in);
        if (rc != 0) throw std::runtime_error("unmarshal failed");
        return obj;
    }

    /// Deserialize into this object. Returns true on success.
    bool unmarshal_into(const std::string& data) {
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_Drawing(in, &data_);
        sstr_free(in);
        return rc == 0;
    }

    /// Access the underlying C struct.
    struct ::Drawing& c_struct() { return data_; }
    const struct ::Drawing& c_struct() const { return data_; }

    bool operator==(const Drawing& other) const { return marshal() == other.marshal(); }
    bool operator!=(const Drawing& other) const { return !(*this == other); }

private:
    struct ::Drawing data_;
};

class AliasNested {
public:
    AliasNested() { AliasNested_init(&data_); }
    ~AliasNested() { AliasNested_clear(&data_); }

    AliasNested(AliasNested&& other) noexcept {
        std::memcpy(&data_, &other.data_, sizeof(data_));
        std::memset(&other.data_, 0, sizeof(other.data_));
    }
    AliasNested& operator=(AliasNested&& other) noexcept {
        if (this != &other) {
            AliasNested_clear(&data_);
            std::memcpy(&data_, &other.data_, sizeof(data_));
            std::memset(&other.data_, 0, sizeof(other.data_));
        }
        return *this;
    }

    AliasNested(const AliasNested& other) {
        AliasNested_init(&data_);
        sstr_t buf = sstr_new();
        json_marshal_indent_AliasNested(const_cast<struct ::AliasNested*>(&other.data_), 0, 0, buf);
        json_unmarshal_AliasNested(buf, &data_);
        sstr_free(buf);
    }
    AliasNested& operator=(const AliasNested& other) {
        if (this != &other) {
            AliasNested_clear(&data_);
            AliasNested_init(&data_);
            sstr_t buf = sstr_new();
            json_marshal_indent_AliasNested(const_cast<struct ::AliasNested*>(&other.data_), 0, 0, buf);
            json_unmarshal_AliasNested(buf, &data_);
            sstr_free(buf);
        }
        return *this;
    }

    // Field accessors
    struct ::Person& info() { return data_.info; }
    const struct ::Person& info() const { return data_.info; }
    struct ::House& addr() { return data_.addr; }
    const struct ::House& addr() const { return data_.addr; }

    /// Serialize to string.
    std::string marshal() const {
        sstr_t buf = sstr_new();
        json_marshal_indent_AliasNested(const_cast<struct ::AliasNested*>(&data_), 0, 0, buf);
        std::string result(sstr_cstr(buf), sstr_length(buf));
        sstr_free(buf);
        return result;
    }

    /// Deserialize from string. Throws on error.
    static AliasNested unmarshal(const std::string& data) {
        AliasNested obj;
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_AliasNested(in, &obj.data_);
        sstr_free(in);
        if (rc != 0) throw std::runtime_error("unmarshal failed");
        return obj;
    }

    /// Deserialize into this object. Returns true on success.
    bool unmarshal_into(const std::string& data) {
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_AliasNested(in, &data_);
        sstr_free(in);
        return rc == 0;
    }

    /// Access the underlying C struct.
    struct ::AliasNested& c_struct() { return data_; }
    const struct ::AliasNested& c_struct() const { return data_; }

    bool operator==(const AliasNested& other) const { return marshal() == other.marshal(); }
    bool operator!=(const AliasNested& other) const { return !(*this == other); }

private:
    struct ::AliasNested data_;
};

class Data {
public:
    Data() { Data_init(&data_); }
    ~Data() { Data_clear(&data_); }

    Data(Data&& other) noexcept {
        std::memcpy(&data_, &other.data_, sizeof(data_));
        std::memset(&other.data_, 0, sizeof(other.data_));
    }
    Data& operator=(Data&& other) noexcept {
        if (this != &other) {
            Data_clear(&data_);
            std::memcpy(&data_, &other.data_, sizeof(data_));
            std::memset(&other.data_, 0, sizeof(other.data_));
        }
        return *this;
    }

    Data(const Data& other) {
        Data_init(&data_);
        sstr_t buf = sstr_new();
        json_marshal_indent_Data(const_cast<struct ::Data*>(&other.data_), 0, 0, buf);
        json_unmarshal_Data(buf, &data_);
        sstr_free(buf);
    }
    Data& operator=(const Data& other) {
        if (this != &other) {
            Data_clear(&data_);
            Data_init(&data_);
            sstr_t buf = sstr_new();
            json_marshal_indent_Data(const_cast<struct ::Data*>(&other.data_), 0, 0, buf);
            json_unmarshal_Data(buf, &data_);
            sstr_free(buf);
        }
        return *this;
    }

    // Field accessors
    struct ::House& house() { return data_.house; }
    const struct ::House& house() const { return data_.house; }
    struct ::Person* people_data() { return data_.people; }
    int people_len() const { return data_.people_len; }

    /// Serialize to string.
    std::string marshal() const {
        sstr_t buf = sstr_new();
        json_marshal_indent_Data(const_cast<struct ::Data*>(&data_), 0, 0, buf);
        std::string result(sstr_cstr(buf), sstr_length(buf));
        sstr_free(buf);
        return result;
    }

    /// Deserialize from string. Throws on error.
    static Data unmarshal(const std::string& data) {
        Data obj;
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_Data(in, &obj.data_);
        sstr_free(in);
        if (rc != 0) throw std::runtime_error("unmarshal failed");
        return obj;
    }

    /// Deserialize into this object. Returns true on success.
    bool unmarshal_into(const std::string& data) {
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_Data(in, &data_);
        sstr_free(in);
        return rc == 0;
    }

    /// Access the underlying C struct.
    struct ::Data& c_struct() { return data_; }
    const struct ::Data& c_struct() const { return data_; }

    bool operator==(const Data& other) const { return marshal() == other.marshal(); }
    bool operator!=(const Data& other) const { return !(*this == other); }

private:
    struct ::Data data_;
};

class NullableNestedStruct {
public:
    NullableNestedStruct() { NullableNestedStruct_init(&data_); }
    ~NullableNestedStruct() { NullableNestedStruct_clear(&data_); }

    NullableNestedStruct(NullableNestedStruct&& other) noexcept {
        std::memcpy(&data_, &other.data_, sizeof(data_));
        std::memset(&other.data_, 0, sizeof(other.data_));
    }
    NullableNestedStruct& operator=(NullableNestedStruct&& other) noexcept {
        if (this != &other) {
            NullableNestedStruct_clear(&data_);
            std::memcpy(&data_, &other.data_, sizeof(data_));
            std::memset(&other.data_, 0, sizeof(other.data_));
        }
        return *this;
    }

    NullableNestedStruct(const NullableNestedStruct& other) {
        NullableNestedStruct_init(&data_);
        sstr_t buf = sstr_new();
        json_marshal_indent_NullableNestedStruct(const_cast<struct ::NullableNestedStruct*>(&other.data_), 0, 0, buf);
        json_unmarshal_NullableNestedStruct(buf, &data_);
        sstr_free(buf);
    }
    NullableNestedStruct& operator=(const NullableNestedStruct& other) {
        if (this != &other) {
            NullableNestedStruct_clear(&data_);
            NullableNestedStruct_init(&data_);
            sstr_t buf = sstr_new();
            json_marshal_indent_NullableNestedStruct(const_cast<struct ::NullableNestedStruct*>(&other.data_), 0, 0, buf);
            json_unmarshal_NullableNestedStruct(buf, &data_);
            sstr_free(buf);
        }
        return *this;
    }

    // Field accessors
    int id() const { return data_.id; }
    void set_id(int v) { data_.id = v; }
    struct ::Person& person() { return data_.person; }
    const struct ::Person& person() const { return data_.person; }
    bool has_person() const { return data_.has_person != 0; }
    enum Color color() const { return static_cast<enum Color>(data_.color); }
    void set_color(enum Color v) { data_.color = v; }
    bool has_color() const { return data_.has_color != 0; }
    void set_has_color(bool v) { data_.has_color = v; }
    enum Status status() const { return static_cast<enum Status>(data_.status); }
    void set_status(enum Status v) { data_.status = v; }
    bool has_status() const { return data_.has_status != 0; }
    void set_has_status(bool v) { data_.has_status = v; }

    /// Serialize to string.
    std::string marshal() const {
        sstr_t buf = sstr_new();
        json_marshal_indent_NullableNestedStruct(const_cast<struct ::NullableNestedStruct*>(&data_), 0, 0, buf);
        std::string result(sstr_cstr(buf), sstr_length(buf));
        sstr_free(buf);
        return result;
    }

    /// Deserialize from string. Throws on error.
    static NullableNestedStruct unmarshal(const std::string& data) {
        NullableNestedStruct obj;
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_NullableNestedStruct(in, &obj.data_);
        sstr_free(in);
        if (rc != 0) throw std::runtime_error("unmarshal failed");
        return obj;
    }

    /// Deserialize into this object. Returns true on success.
    bool unmarshal_into(const std::string& data) {
        sstr_t in = sstr_of(data.data(), data.size());
        int rc = json_unmarshal_NullableNestedStruct(in, &data_);
        sstr_free(in);
        return rc == 0;
    }

    /// Access the underlying C struct.
    struct ::NullableNestedStruct& c_struct() { return data_; }
    const struct ::NullableNestedStruct& c_struct() const { return data_; }

    bool operator==(const NullableNestedStruct& other) const { return marshal() == other.marshal(); }
    bool operator!=(const NullableNestedStruct& other) const { return !(*this == other); }

private:
    struct ::NullableNestedStruct data_;
};

} // namespace jgc
